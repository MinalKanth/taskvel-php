<!DOCTYPE html>
<html lang="en-IN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
<!-- ============================= PRIMARY SEO ============================= -->
<title>Best GST &amp; Business Registration Consultancy in Assam | Samal Consultancy</title>
<meta name="description" content="GST, EPFO/ESIC, company & trademark registration across Nagaon, Tezpur, Guwahati and Assam. 30+ years, 70+ businesses trusted. Book a free consultation.">
<link rel="canonical" href="https://www.samalconsultancy.com/">
<meta name="robots" content="index, follow, max-image-preview:large, max-snippet:-1">
<meta name="theme-color" content="#0A1128">
<meta name="author" content="Samal Consultancy">
<!-- Favicon -->
<link rel="icon" href="images/favicon.ico" type="image/x-icon">
<link rel="shortcut icon" href="images/favicon.ico" type="image/x-icon">
<link rel="apple-touch-icon" href="images/favicon.ico">
 
<!-- Local SEO hints -->
<meta name="geo.region" content="IN-AS">
<meta name="geo.placename" content="Nagaon">
<meta name="geo.position" content="26.3480;92.6840">
<meta name="ICBM" content="26.3480, 92.6840">
 
<!-- ============================= OPEN GRAPH ============================= -->
<meta property="og:type" content="website">
<meta property="og:site_name" content="Samal Consultancy">
<meta property="og:title" content="Best GST &amp; Business Registration Consultancy in Assam | Samal Consultancy">
<meta property="og:description" content="Trusted GST registration, EPFO/ESIC compliance, company registration, trademark filing and tax advisory across Nagaon, Tezpur, Guwahati and Assam. 30+ years, 70+ businesses served.">
<meta property="og:url" content="https://www.samalconsultancy.com/">
<meta property="og:image" content="https://www.samalconsultancy.com/assets/images/og-cover.jpg">
<meta property="og:image:width" content="1200">
<meta property="og:image:height" content="630">
<meta property="og:locale" content="en_IN">
 
<!-- ============================= TWITTER CARD ============================= -->
<meta name="twitter:card" content="summary_large_image">
<meta name="twitter:title" content="Best GST &amp; Business Registration Consultancy in Assam">
<meta name="twitter:description" content="Trusted compliance and tax advisory since 1993. GST, EPFO/ESIC, company registration, trademarks, payroll and more — Nagaon, Tezpur, Guwahati and across Assam.">
<meta name="twitter:image" content="https://www.samalconsultancy.com/assets/images/og-cover.jpg">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@500;600;700&family=Inter:wght@400;500;600;700&family=Plus+Jakarta+Sans:wght@500;600&display=swap" rel="stylesheet">
<style>
/* ============================= TOKENS ============================= */
:root{
  --amber:#C9A227;
  --amber-2:#E8C766;
  --amber-deep:#8F7112;
  --teal:#1B2A6B;
  --teal-2:#8FA0E8;
  --teal-deep:#12513E;
  --ivory:#FAF8F3;
  --paper:#F3F1E9;
  --charcoal:#1C1C1A;
  --navy-ink:#0A1128;
  --navy-2:#0F4436;
  --silver:#D8D6CE;
  --line:rgba(28,28,26,0.10);
  --line-invert:rgba(255,255,255,0.14);
  --shadow-lg: 0 30px 80px -30px rgba(10,17,40,0.35);
  --shadow-sm: 0 10px 30px -12px rgba(10,17,40,0.18);
  --radius-lg: 22px;
  --radius-md: 14px;
  --ease: cubic-bezier(.22,1,.36,1);
  --font-display:'Space Grotesk', sans-serif;
  --font-body:'Inter', sans-serif;
  --font-eyebrow:'Plus Jakarta Sans', sans-serif;
  --mesh-1: rgba(232,199,102,0.10);
  --mesh-2: rgba(143,160,232,0.12); 
}

*{box-sizing:border-box;}
html{scroll-behavior:smooth;}
@media (prefers-reduced-motion: reduce){
  html{scroll-behavior:auto;}
  *{animation-duration:0.001ms !important; animation-iteration-count:1 !important; transition-duration:0.001ms !important;}
}
body{
  margin:0;
  font-family:var(--font-body);
  background:var(--ivory);
  color:var(--charcoal);
  -webkit-font-smoothing:antialiased;
  overflow-x:hidden;
}
img{max-width:100%;display:block;}
a{color:inherit;}
ul{margin:0;padding:0;}
button{font-family:inherit;}
::selection{background:var(--teal-2); color:var(--navy-ink);}

/* Mobile-first container: compact padding by default, more room on larger screens */
.wrap{max-width:1240px;margin:0 auto;padding:0 20px;}
@media (min-width:640px){ .wrap{padding:0 28px;} }
@media (min-width:1024px){ .wrap{padding:0 32px;} }

.eyebrow{
  font-family:var(--font-eyebrow);
  font-size:12px;
  font-weight:600;
  letter-spacing:.12em;
  text-transform:uppercase;
  display:inline-flex;
  align-items:center;
  gap:8px;
  color:var(--teal-deep);
}
.eyebrow::before{
  content:'';
  width:18px;height:1px;
  background:var(--amber);
}

h1,h2,h3,h4{font-family:var(--font-display); margin:0; letter-spacing:-0.01em; color:var(--navy-ink);}
p{line-height:1.7; color:#54514C; margin:0;}

.section-head{max-width:640px; margin-bottom:40px;}
.section-head h2{font-size:clamp(26px,5vw,44px); margin-top:14px; line-height:1.18;}
.section-head p{margin-top:14px; font-size:16px;}
.section-head.center{margin-left:auto;margin-right:auto;text-align:center;}
@media (min-width:768px){ .section-head{margin-bottom:56px;} .section-head p{font-size:17px;} }

section{position:relative;}
.pad{padding:64px 0;}
@media (min-width:768px){ .pad{padding:100px 0;} }
@media (min-width:1100px){ .pad{padding:120px 0;} }

/* Buttons */
.btn{
  position:relative;
  overflow:hidden;
  display:inline-flex; align-items:center; justify-content:center; gap:10px;
  padding:15px 26px; font-weight:600; font-size:14.5px;
  border-radius:100px; text-decoration:none; cursor:pointer;
  border:1px solid transparent; transition:transform .35s var(--ease), box-shadow .35s var(--ease), background .3s ease, color .3s ease;
  white-space:nowrap; -webkit-tap-highlight-color:transparent;
}
@media (min-width:640px){ .btn{padding:16px 30px; font-size:15px;} }
.btn:active{transform:scale(.96);}
.btn-gold{
  background:linear-gradient(135deg,var(--amber-2),var(--amber));
  color:#fff;
  box-shadow:0 14px 30px -10px rgba(224,108,31,0.55);
}
.btn-gold:hover{transform:translateY(-3px); box-shadow:0 20px 40px -12px rgba(224,108,31,0.6);}
.btn-ghost{
  background:transparent; color:var(--ivory); border-color:rgba(255,255,255,0.35);
}
.btn-ghost:hover{background:rgba(255,255,255,0.10); border-color:rgba(255,255,255,0.6);}
.btn-outline-dark{
  background:transparent;color:var(--teal-deep);border-color:rgba(14,124,140,0.3);
}
.btn-outline-dark:hover{background:var(--teal-deep); color:var(--ivory); border-color:var(--teal-deep);}

/* Ripple effect (JS adds .ripple span) */
.ripple-span{
  position:absolute; border-radius:50%; background:rgba(255,255,255,0.55);
  transform:scale(0); animation:rippleAnim .6s ease-out forwards; pointer-events:none;
}
@keyframes rippleAnim{ to{ transform:scale(2.6); opacity:0; } }

/* ============================= NAV ============================= */
.navbar{
  position:fixed; top:0; left:0; right:0; z-index:999;
  padding:16px 0;
  transition:padding .35s var(--ease), background .35s var(--ease), box-shadow .35s var(--ease);
}
.navbar.scrolled{
  padding:10px 0;
  background:rgba(250,247,242,0.85);
  backdrop-filter:blur(16px) saturate(160%);
  box-shadow:0 8px 30px -14px rgba(27,31,39,0.18);
}
.nav-inner{display:flex; align-items:center; justify-content:space-between; gap:16px;}
.brand{display:flex; align-items:center; gap:10px; text-decoration:none; min-width:0;}
.brand-logo{
  height:36px; width:auto; max-width:150px; object-fit:contain; flex-shrink:0;
}
.brand-mark{
  width:36px;height:36px;border-radius:10px; flex-shrink:0;
  background:linear-gradient(145deg,var(--amber),var(--teal-deep));
  display:none; align-items:center;justify-content:center;
  color:#fff; font-family:var(--font-display); font-weight:700; font-size:15px;
  box-shadow: inset 0 0 0 1px rgba(255,255,255,0.15);
}
.brand-name{font-family:var(--font-display); font-weight:700; font-size:16.5px; color: #E06C1F; letter-spacing:-.01em; white-space:nowrap; overflow:hidden; text-overflow:ellipsis;}

.brand-mark {
    width: 42px;
    height: 42px;
    border-radius: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, #ff8c00, #ff6a00);
    color: #fff;
    font-weight: 700;
    font-size: 1rem;
}

.brand-name {
    color: #ff7a00;
    font-size: 1.2rem;
    font-weight: 700;
    margin-left: 10px;
}


#navLogoFallback {
    display: none;
    align-items: center;
    justify-content: center;
}

/* Mobile */
@media (max-width: 768px) {
    .brand-name {
        display: none;
    }
}

@media (min-width:480px){ .brand-name{font-size:18px;} }

.nav-links{list-style:none; display:flex; align-items:center; gap:30px;}
.nav-links a{
  text-decoration:none; font-size:14.5px; font-weight:600; letter-spacing:.01em;
  position:relative; padding:6px 0;
}
.nav-links a::after{
  content:''; position:absolute; left:0; bottom:0; height:1.5px; width:0%;
  background:var(--amber); transition:width .35s var(--ease);
}
.nav-links a:hover::after{width:100%;}
.nav-cta{display:flex; align-items:center; gap:14px;}
.menu-toggle{display:flex; flex-direction:column; gap:5px; cursor:pointer; background:none;border:none; padding:8px; position:relative; z-index:1000; -webkit-tap-highlight-color:transparent;}
.menu-toggle span{width:22px;height:2px;background:currentColor;border-radius:2px;transition:all .3s ease; transform-origin:left center;}
.menu-toggle.open span:nth-child(1){transform:rotate(45deg);}
.menu-toggle.open span:nth-child(2){opacity:0; transform:translateX(-8px);}
.menu-toggle.open span:nth-child(3){transform:rotate(-45deg);}
.menu-toggle.open{color:var(--ivory) !important;}

.navbar:not(.scrolled) .nav-links a, .navbar:not(.scrolled) .menu-toggle{color:var(--ivory);}
.navbar.scrolled .nav-links a, .navbar.scrolled .menu-toggle{color:var(--navy-ink);}

/* Mobile-first: nav links hidden off-canvas by default, revealed as horizontal bar on larger screens */
.nav-links{
  position:fixed; inset:0 0 0 auto; width:min(320px,82%); height:100dvh;
  background:linear-gradient(180deg,var(--navy-ink),var(--teal-deep));
  flex-direction:column; justify-content:center; align-items:flex-start;
  padding:40px; gap:26px; transform:translateX(100%); transition:transform .45s var(--ease);
  box-shadow:-20px 0 60px rgba(0,0,0,0.25);
}
.nav-links.open{transform:translateX(0);}
.nav-links a{color:var(--ivory) !important; font-size:19px;}
.nav-cta .btn-gold{padding:12px 20px; font-size:13px;}

@media (min-width:901px){
  .nav-links{
    position:static; width:auto; height:auto; background:none; flex-direction:row;
    padding:0; gap:34px; transform:none; box-shadow:none;
  }
  .nav-links a{font-size:14.5px !important;}
  .menu-toggle{display:none;}
  .navbar:not(.scrolled) .nav-links a{color:var(--ivory) !important;}
  .navbar.scrolled .nav-links a{color:var(--navy-ink) !important;}
  .nav-cta .btn-gold{padding:14px 26px; font-size:14px;}
}

/* Mobile sticky bottom CTA */
.mobile-cta-bar{
  position:fixed; left:0; right:0; bottom:0; z-index:850;
  display:flex; gap:10px; padding:12px 16px calc(12px + env(safe-area-inset-bottom));
  background:rgba(250,247,242,0.92); backdrop-filter:blur(14px);
  border-top:1px solid var(--line);
  transform:translateY(100%); transition:transform .4s var(--ease);
}
.mobile-cta-bar.show{transform:translateY(0);}
.mobile-cta-bar a{flex:1; text-align:center;}
@media (min-width:901px){ .mobile-cta-bar{display:none;} }

/* ============================= HERO ============================= */
.hero{
  position:relative;
  min-height:100dvh;
  display:flex; align-items:center;
  background:
    radial-gradient(1100px 560px at 88% -10%, rgba(232,199,102,0.14), transparent 60%),
    radial-gradient(900px 500px at -10% 110%, rgba(27,42,107,0.35), transparent 60%),
    linear-gradient(160deg, var(--navy-ink) 0%, var(--teal-deep) 65%, #0F4436 100%);
  padding:110px 0 100px;
  overflow:hidden;
}

.hero-orbs{position:absolute; inset:0; overflow:hidden; pointer-events:none; z-index:0;}
.orb{position:absolute; border-radius:50%; filter:blur(70px); opacity:.32; animation:orbFloat 16s ease-in-out infinite;}
.orb-1{width:300px;height:300px; background:var(--amber-2); top:-70px; right:6%; animation-duration:17s;}
.orb-2{width:230px;height:230px; background:var(--teal-2); bottom:-50px; left:4%; animation-duration:20s; animation-delay:2s;}
.orb-3{width:170px;height:170px; background:var(--amber); top:42%; left:48%; animation-duration:23s; animation-delay:4s;}
@keyframes orbFloat{0%,100%{transform:translate(0,0) scale(1);} 50%{transform:translate(28px,-26px) scale(1.14);}}
@media (prefers-reduced-motion: reduce){ .orb{animation:none;} }
.cursor-glow{
  position:absolute; width:360px; height:360px; border-radius:50%;
  pointer-events:none; z-index:0; left:50%; top:50%;
  background:radial-gradient(circle, rgba(232,199,102,0.16), transparent 70%);
  transform:translate(-50%,-50%); opacity:0; transition:opacity .5s ease; will-change:transform;
}
.hero:hover .cursor-glow{opacity:1;}
.hero-grid{
  position:relative; z-index:1;
  display:grid; grid-template-columns:1fr; gap:44px; align-items:center;
}
@media (min-width:980px){ .hero-grid{grid-template-columns:1.05fr .95fr; gap:60px;} }

.est-badge{
  display:inline-flex; align-items:center; gap:8px; margin-bottom:14px;
  background:rgba(255,255,255,0.08); border:1px solid rgba(255,255,255,0.18);
  padding:7px 14px; border-radius:100px; font-family:var(--font-eyebrow);
  font-size:11.5px; font-weight:600; letter-spacing:.05em; color:var(--teal-2); text-transform:uppercase;
}
.est-badge b{color:var(--amber-2);}

.hero-content .eyebrow{color:var(--amber-2);}
.hero-content .eyebrow::before{background:var(--amber-2);}
.hero-content h1{
  color:var(--ivory);
  font-size:clamp(32px,8vw,66px);
  line-height:1.08;
  margin-top:16px;
  letter-spacing:-0.02em;
}
.hero-content h1 em{
  font-style:normal;
  background:linear-gradient(120deg,var(--amber-2),var(--teal-2),var(--amber));
  background-size:200% auto;
  -webkit-background-clip:text; background-clip:text; color:transparent;
  animation:gradientShift 6s ease-in-out infinite;
}
@keyframes gradientShift{
  0%{background-position:0% 50%;}
  50%{background-position:100% 50%;}
  100%{background-position:0% 50%;}
}
@media (prefers-reduced-motion: reduce){ .hero-content h1 em{animation:none;} }
.hero-content p.lead{
  color:rgba(250,247,242,0.78); font-size:16.5px; max-width:520px; margin-top:18px;
}
@media (min-width:640px){ .hero-content p.lead{font-size:18px;} }
.hero-tagline{
  font-family:var(--font-display); font-weight:600; font-size:15px; color:var(--teal-2);
  margin-top:10px; letter-spacing:.01em;
}
.hero-actions{display:flex; gap:14px; margin-top:32px; flex-wrap:wrap;}
.hero-actions .btn{flex:1 1 auto; min-width:150px;}
@media (min-width:480px){ .hero-actions .btn{flex:0 1 auto;} }

.trust-row{
  display:grid; grid-template-columns:repeat(3,1fr); gap:20px; margin-top:44px;
  border-top:1px solid rgba(255,255,255,0.14); padding-top:24px;
}
.trust-row .t-num{font-family:var(--font-display); font-size:22px; font-weight:700; color:var(--ivory);}
@media (min-width:480px){ .trust-row .t-num{font-size:28px;} }
.trust-row .t-label{font-size:11.5px; color:rgba(250,247,242,0.6); margin-top:4px; letter-spacing:.02em;}

/* Signature element: Live Ledger */
.hero-visual{perspective:1200px;}
.ledger-card{
  background:rgba(255,255,255,0.06);
  border:1px solid rgba(255,255,255,0.16);
  border-radius:var(--radius-lg);
  backdrop-filter:blur(18px);
  padding:22px 18px 20px;
  box-shadow:var(--shadow-lg);
  position:relative;
  transform-style:preserve-3d;
  transition:transform .35s cubic-bezier(.22,1,.36,1);
  will-change:transform;
}
@media (min-width:640px){ .ledger-card{padding:26px 24px 22px;} }
.ledger-card::before{
  content:''; position:absolute; inset:-1px; border-radius:inherit; padding:1px;
  background:linear-gradient(140deg, rgba(95,211,227,0.55), transparent 40%, rgba(255,255,255,0.08));
  -webkit-mask:linear-gradient(#000 0 0) content-box, linear-gradient(#000 0 0);
  -webkit-mask-composite:xor; mask-composite:exclude;
  pointer-events:none;
}
.ledger-top{display:flex; align-items:center; justify-content:space-between; margin-bottom:16px;}
.ledger-title{color:var(--ivory); font-family:var(--font-display); font-weight:600; font-size:14px; display:flex; gap:9px; align-items:center;}
.ledger-dot{width:8px;height:8px;border-radius:50%;background:#4ADE80; box-shadow:0 0 0 4px rgba(74,222,128,0.18); animation:pulse 2s infinite;}
@keyframes pulse{0%,100%{opacity:1;}50%{opacity:.4;}}
.ledger-clock{color:rgba(250,247,242,0.55); font-size:11.5px; font-variant-numeric:tabular-nums;}
.ledger-rows{display:flex; flex-direction:column; gap:9px;}
.ledger-row{
  display:grid; grid-template-columns:auto 1fr auto; gap:10px; align-items:center;
  background:rgba(255,255,255,0.04); border:1px solid rgba(255,255,255,0.08);
  border-radius:12px; padding:11px 12px;
  opacity:0; transform:translateY(8px);
  animation:rowIn .6s var(--ease) forwards;
}
@media (min-width:480px){ .ledger-row{grid-template-columns:auto 1fr auto auto; padding:12px 14px;} }
@keyframes rowIn{to{opacity:1; transform:translateY(0);}}
.ledger-check{
  width:20px;height:20px;border-radius:50%; display:flex;align-items:center;justify-content:center;
  background:rgba(74,222,128,0.15); color:#4ADE80; font-size:11px; flex-shrink:0;
}
.ledger-check.pending{background:rgba(245,130,30,0.18); color:var(--amber-2);}
.ledger-name{color:var(--ivory); font-size:12.5px; font-weight:500; min-width:0;}
@media (min-width:480px){ .ledger-name{font-size:13.5px;} }
.ledger-name small{display:block; color:rgba(250,247,242,0.5); font-size:10.5px; font-weight:400; margin-top:2px;}
.ledger-status{font-size:10.5px; font-weight:600; letter-spacing:.03em; text-transform:uppercase; color:#4ADE80; text-align:right;}
.ledger-status.pending{color:var(--amber-2);}
.ledger-time{display:none; color:rgba(250,247,242,0.45); font-size:11px; font-variant-numeric:tabular-nums;}
@media (min-width:480px){ .ledger-time{display:block;} }
.ledger-foot{
  margin-top:16px; padding-top:14px; border-top:1px dashed rgba(255,255,255,0.14);
  display:flex; justify-content:space-between; align-items:center; gap:10px; flex-wrap:wrap;
}
.ledger-foot .score{color:var(--amber-2); font-family:var(--font-display); font-weight:700; font-size:18px;}
.ledger-foot .score-label{color:rgba(250,247,242,0.55); font-size:10.5px;}

/* ============================= HERO PREMIUM 3D ENHANCEMENTS ============================= */
.hero-content{
  transform-style:preserve-3d;
  will-change:transform;
  transition:transform .5s var(--ease);
}
.hero-orbs .orb{ transition:transform .4s var(--ease); will-change:transform; }

.hero-particles{position:absolute; inset:0; pointer-events:none; z-index:0; overflow:hidden;}
.hero-particle{
  position:absolute; border-radius:50%;
  filter:blur(.5px);
  opacity:.6;
  animation-name:particleDrift;
  animation-timing-function:ease-in-out;
  animation-iteration-count:infinite;
}
@keyframes particleDrift{
  0%{ transform:translate3d(0,0,0) scale(1); opacity:.4; }
  50%{ transform:translate3d(16px,-26px,0) scale(1.4); opacity:.85; }
  100%{ transform:translate3d(0,0,0) scale(1); opacity:.4; }
}
@media (prefers-reduced-motion: reduce){ .hero-particle{animation:none;} }

.btn[data-ripple]{ position:relative; overflow:hidden; }
.btn-shine{
  position:absolute; top:0; left:-60%; width:40%; height:100%;
  background:linear-gradient(120deg, transparent, rgba(255,255,255,0.55), transparent);
  transform:skewX(-20deg);
  pointer-events:none;
}
.btn[data-ripple]:hover .btn-shine{ animation:shineSweep 1s var(--ease); }
@keyframes shineSweep{ from{ left:-60%; } to{ left:130%; } }

.ledger-card{
  box-shadow:var(--shadow-lg), 0 1px 0 rgba(255,255,255,0.12) inset;
  position:relative;
}
.ledger-card::after{
  content:''; position:absolute; inset:0; border-radius:inherit; pointer-events:none; z-index:2;
  background:radial-gradient(420px circle at var(--gx,50%) var(--gy,30%), rgba(255,255,255,0.12), transparent 60%);
  opacity:0; transition:opacity .4s ease;
}
.ledger-card:hover::after{ opacity:1; }

/* ============================= MARQUEE ============================= */
.marquee-band{
  background:var(--paper); border-top:1px solid var(--line); border-bottom:1px solid var(--line);
  padding:18px 0; overflow:hidden;
}
@media (min-width:768px){ .marquee-band{padding:22px 0;} }
.marquee-track{display:flex; gap:44px; width:max-content; animation:scroll 28s linear infinite;}
@media (min-width:768px){ .marquee-track{gap:64px; animation-duration:32s;} }
.marquee-band:hover .marquee-track{animation-play-state:paused;}
@keyframes scroll{from{transform:translateX(0);} to{transform:translateX(-50%);}}
.marquee-item{
  font-family:var(--font-eyebrow); font-weight:600; font-size:12px; letter-spacing:.07em; text-transform:uppercase;
  color:#8F8B80; display:flex; align-items:center; gap:12px; white-space:nowrap;
}
@media (min-width:768px){ .marquee-item{font-size:13px;} }
.marquee-item::after{content:'◆'; color:var(--amber); font-size:7px;}

/* ============================= ABOUT ============================= */
.about-grid{display:grid; grid-template-columns:1fr; gap:40px; align-items:start;}
@media (min-width:900px){ .about-grid{grid-template-columns:.85fr 1.15fr; gap:70px;} }
.about-visual{
  position:relative; border-radius:var(--radius-lg); overflow:hidden;
  background:linear-gradient(160deg,var(--teal-deep),var(--navy-ink));
  aspect-ratio:4/5; padding:26px; display:flex; flex-direction:column; justify-content:flex-end;
  box-shadow:var(--shadow-sm);
  transform-style:preserve-3d;
}
@media (min-width:640px){ .about-visual{padding:32px;} }
.about-visual .quote-mark{position:absolute; top:22px; left:24px; font-family:var(--font-display); font-size:56px; color:rgba(255,255,255,0.15); line-height:1;}
.about-visual{ overflow:hidden; }
.about-visual::before{
  content:''; position:absolute; inset:0; z-index:0; pointer-events:none;
  background:linear-gradient(120deg, transparent 30%, rgba(255,255,255,0.14) 48%, transparent 65%);
  transform:translateX(-130%); transition:transform .9s var(--ease);
}
.about-visual:hover::before{ transform:translateX(130%); }
.about-visual > *{ position:relative; z-index:1; }
@media (min-width:640px){ .about-visual .quote-mark{font-size:70px; top:28px; left:28px;} }
.about-visual p{color:var(--ivory); font-size:17px; line-height:1.6; font-family:var(--font-display); font-weight:500;}
@media (min-width:640px){ .about-visual p{font-size:19px;} }
.about-visual .sig{margin-top:16px; color:var(--amber-2); font-size:12.5px; font-weight:600; letter-spacing:.03em;}

.value-list{display:grid; gap:0; margin-top:30px;}
.value-item{
  display:grid; grid-template-columns:44px 1fr; gap:16px; padding:20px 0;
  border-top:1px solid var(--line);
}
@media (min-width:640px){ .value-item{grid-template-columns:56px 1fr; gap:20px; padding:22px 0;} }
.value-item:last-child{border-bottom:1px solid var(--line);}
.value-num{font-family:var(--font-display); font-weight:600; color:var(--amber); font-size:13px;}
.value-item h4{font-size:17px; margin-bottom:6px;}
.value-item p{font-size:14.5px;}

.trust-orbit-wrap{ height:180px; perspective:900px; display:flex; align-items:center; justify-content:center; margin:24px 0; }
.trust-orbit{ position:relative; width:140px; height:140px; transform-style:preserve-3d; animation:orbitSpin 14s linear infinite; }
@keyframes orbitSpin{ from{ transform:rotateY(0deg) rotateX(18deg); } to{ transform:rotateY(360deg) rotateX(18deg); } }
@media (prefers-reduced-motion: reduce){ .trust-orbit{animation:none;} }
.trust-orbit-hub{
  position:absolute; inset:0; margin:auto; width:56px; height:56px; border-radius:50%;
  background:linear-gradient(145deg,var(--amber-2),var(--amber));
  display:flex; align-items:center; justify-content:center; color:#fff; font-family:var(--font-display); font-weight:700; font-size:13px;
  box-shadow:0 0 30px rgba(201,162,39,0.5);
  transform:translateZ(0);
}
.trust-orbit-node{
  position:absolute; top:50%; left:50%; width:38px; height:38px; margin:-19px;
  border-radius:50%; background:var(--ivory); border:1px solid var(--line);
  display:flex; align-items:center; justify-content:center; font-size:15px; color:var(--teal-deep);
  box-shadow:var(--shadow-sm);
}

/* ============================= STATS ============================= */
.stats-band{
  background:var(--navy-ink); color:var(--ivory);
  padding:50px 0;
}
@media (min-width:768px){ .stats-band{padding:70px 0;} }
.stats-grid{display:grid; grid-template-columns:repeat(2,1fr); gap:28px;}
@media (min-width:768px){ .stats-grid{grid-template-columns:repeat(4,1fr); gap:40px;} }
.stat-block{text-align:left; border-left:2px solid var(--amber); padding-left:14px;}
@media (min-width:640px){ .stat-block{padding-left:18px;} }
.stat-num{font-family:var(--font-display); font-size:clamp(24px,6vw,44px); font-weight:700; color:var(--ivory); perspective:400px;}
.stat-num .digit{
  display:inline-block; transform-style:preserve-3d; transition:transform .5s var(--ease);
  text-shadow:0 0 18px rgba(232,199,102,0.35);
}
.stat-block{
  position:relative;
  transition:transform .25s var(--ease), filter .25s var(--ease);
}
.stat-block.settle{ filter:drop-shadow(0 10px 20px rgba(0,0,0,0.25)); }
.stat-block::before{
  content:''; position:absolute; left:14px; right:0; bottom:-10px; height:2px;
  background:linear-gradient(90deg, var(--amber-2), transparent);
  transform:scaleX(0); transform-origin:left; transition:transform .6s var(--ease);
}
.stat-block.settle::before{ transform:scaleX(1); }
.stat-num span{color:var(--teal-2);}
.stat-label{color:rgba(250,247,242,0.6); font-size:12.5px; margin-top:6px; letter-spacing:.01em;}

/* ============================= CORE SERVICES ============================= */
.services-bg{
  background:var(--paper);
  background-image:
    radial-gradient(600px 300px at 10% 20%, var(--mesh-1), transparent 60%),
    radial-gradient(500px 300px at 90% 80%, var(--mesh-2), transparent 60%);
  background-attachment:fixed;
}
.core-grid{display:grid; grid-template-columns:1fr; gap:18px;}
@media (min-width:560px){ .core-grid{grid-template-columns:repeat(2,1fr);} }
@media (min-width:1024px){ .core-grid{grid-template-columns:repeat(4,1fr); gap:24px;} }
.core-card{
  background:var(--ivory); border:1px solid var(--line); border-radius:var(--radius-md);
  padding:28px 22px; position:relative; overflow:hidden;
  transition:transform .4s var(--ease), box-shadow .4s var(--ease), border-color .3s ease;
  -webkit-tap-highlight-color:transparent;
}
@media (min-width:640px){ .core-card{padding:32px 26px;} }
.core-card::before{
  content:''; position:absolute; top:0; left:0; right:0; height:3px;
  background:linear-gradient(90deg,var(--amber),var(--teal));
  transform:scaleX(0); transform-origin:left; transition:transform .5s var(--ease);
}
.core-card:hover, .core-card:active{transform:translateY(-6px); box-shadow:var(--shadow-sm); border-color:transparent;}
.core-card::after{
  content:''; position:absolute; inset:-1px; border-radius:inherit; padding:1.5px; z-index:2;
  background:conic-gradient(from var(--ang), var(--amber-2), transparent 25%, var(--teal-2) 50%, transparent 75%, var(--amber-2));
  -webkit-mask:linear-gradient(#000 0 0) content-box, linear-gradient(#000 0 0);
  -webkit-mask-composite:xor; mask-composite:exclude;
  opacity:0; transition:opacity .4s ease; animation:spinBorder 5s linear infinite; pointer-events:none;
}
.core-card:hover::after{ opacity:1; }
.core-card:hover::before, .core-card:active::before{transform:scaleX(1);}
.core-icon{
  width:48px;height:48px; border-radius:14px; background:var(--teal-deep); color:var(--teal-2);
  display:flex; align-items:center; justify-content:center; font-size:19px; margin-bottom:18px;
  transition:transform .45s var(--ease);
}
@media (min-width:640px){ .core-icon{width:52px; height:52px; font-size:20px; margin-bottom:22px;} }
.core-card:hover .core-icon, .core-card:active .core-icon{transform:rotate(-8deg) scale(1.06);}
.core-card h3{font-size:18px; margin-bottom:10px; line-height:1.3;}
@media (min-width:640px){ .core-card h3{font-size:19px;} }
.core-card p{font-size:14px; margin-bottom:18px;}
.core-link{
  font-size:13.5px; font-weight:600; color:var(--teal-deep); text-decoration:none;
  display:inline-flex; align-items:center; gap:6px;
}
.core-link .arrow{transition:transform .3s var(--ease);}
.core-card:hover .arrow, .core-card:active .arrow{transform:translateX(4px);}

/* ============================= DETAILED SERVICES ACCORDION ============================= */
.acc-list{border-top:1px solid var(--line);}
.acc-item{border-bottom:1px solid var(--line);}
.acc-head{
  width:100%; background:none; border:none; text-align:left; cursor:pointer;
  display:grid; grid-template-columns:32px 1fr auto; align-items:center; gap:12px;
  padding:20px 4px; color:inherit; -webkit-tap-highlight-color:transparent;
}
@media (min-width:640px){ .acc-head{grid-template-columns:44px 1fr auto auto; gap:20px; padding:26px 8px;} }
.acc-index{font-family:var(--font-display); color:var(--amber); font-weight:600; font-size:13px;}
.acc-head h4{font-size:16.5px; font-weight:600;}
@media (min-width:640px){ .acc-head h4{font-size:20px;} }
.acc-count{display:none; font-size:12.5px; color:#8F8B80; font-family:var(--font-eyebrow); letter-spacing:.03em;}
@media (min-width:640px){ .acc-count{display:block;} }
.acc-plus{
  width:30px;height:30px; border-radius:50%; border:1px solid var(--line);
  display:flex; align-items:center; justify-content:center; position:relative; flex-shrink:0;
  transition:background .3s ease, border-color .3s ease, transform .4s var(--ease);
}
@media (min-width:640px){ .acc-plus{width:34px; height:34px;} }
.acc-plus::before, .acc-plus::after{
  content:''; position:absolute; background:var(--navy-ink); transition:transform .35s var(--ease), background .3s ease;
}
.acc-plus::before{width:12px;height:1.4px;}
.acc-plus::after{width:1.4px;height:12px;}
.acc-item.open .acc-plus{background:var(--teal-deep); border-color:var(--teal-deep);}
.acc-item.open .acc-plus::before, .acc-item.open .acc-plus::after{background:var(--ivory);}
.acc-item.open .acc-plus::after{transform:rotate(90deg) scaleY(0);}
.acc-panel{max-height:0; overflow:hidden; transition:max-height .55s var(--ease);}
.acc-panel-inner{padding:0 4px 26px 44px; display:flex; flex-wrap:wrap; gap:8px;}
@media (min-width:640px){ .acc-panel-inner{padding:0 8px 34px 64px; gap:10px;} }
.chip{
  font-size:13px; padding:8px 14px; border-radius:100px; background:var(--paper);
  border:1px solid var(--line); color:#403C36; transition:all .3s ease; -webkit-tap-highlight-color:transparent;
}
@media (min-width:640px){ .chip{font-size:13.5px; padding:9px 16px;} }
.chip:hover, .chip:active{background:var(--teal-deep); color:var(--ivory); border-color:var(--teal-deep); transform:translateY(-2px);}

/* ============================= WHY US ============================= */
.why-grid{display:grid; grid-template-columns:1fr; gap:1px; background:var(--line); border:1px solid var(--line); border-radius:var(--radius-md); overflow:hidden;}
@media (min-width:520px){ .why-grid{grid-template-columns:repeat(2,1fr);} }
@media (min-width:900px){ .why-grid{grid-template-columns:repeat(4,1fr);} }
.why-cell{background:var(--ivory); padding:30px 22px; transition:background .35s ease; -webkit-tap-highlight-color:transparent; perspective:700px;}
@media (min-width:640px){ .why-cell{padding:38px 28px;} }
.why-cell:hover, .why-cell:active{background:var(--teal-deep);}
.why-cell:hover *, .why-cell:active *{color:var(--ivory) !important;}
.why-cell:hover .why-icon, .why-cell:active .why-icon{color:var(--amber-2) !important; transform:scale(1.1);}
.why-icon{font-size:20px; color:var(--teal-deep); margin-bottom:16px; display:block; transition:transform .35s var(--ease);}
@media (min-width:640px){ .why-icon{font-size:22px; margin-bottom:18px;} }
.why-cell h4{font-size:16px; margin-bottom:8px;}
.why-cell p{font-size:13.5px;}

/* ============================= CORE SERVICES + WHY US PREMIUM 3D ============================= */
.core-card, .why-cell{
  transform-style:preserve-3d;
  will-change:transform;
  position:relative;
}
.core-card{ perspective:800px; }
.core-glare{
  position:absolute; inset:0; z-index:0; pointer-events:none; border-radius:inherit;
  background:radial-gradient(300px circle at var(--mx,50%) var(--my,40%), rgba(232,199,102,0.14), transparent 60%);
  opacity:0; transition:opacity .4s ease;
}
.core-card:hover .core-glare{ opacity:1; }
.core-card > *:not(.core-glare){ position:relative; z-index:1; }

.core-icon{ box-shadow:0 10px 24px -12px rgba(10,17,40,0.35); }
.core-card:hover .core-icon{
  transform:rotate(-8deg) scale(1.08) translateZ(24px);
  box-shadow:0 18px 32px -14px rgba(201,162,39,0.45);
}

.why-cell{ overflow:hidden; }
.why-cell::before{
  content:''; position:absolute; inset:0; z-index:0; pointer-events:none;
  background:linear-gradient(120deg, transparent 30%, rgba(255,255,255,0.14) 48%, transparent 65%);
  transform:translateX(-120%);
  transition:transform .8s var(--ease);
}
.why-cell:hover::before{ transform:translateX(120%); }
.why-cell > *{ position:relative; z-index:1; }
.why-cell .why-icon{
  display:inline-flex; align-items:center; justify-content:center;
  width:44px; height:44px; border-radius:12px;
  background:rgba(14,124,140,0.08);
  transition:transform .4s var(--ease), background .35s ease;
}
.why-cell:hover .why-icon{ transform:rotate(-6deg) scale(1.12) translateZ(20px); background:rgba(255,255,255,0.14); }

/* ============================= PROCESS ============================= */
.process-strip{position:relative;}
.process-track{
  display:grid; grid-template-columns:1fr; gap:30px; position:relative; margin-top:16px;
}
@media (min-width:900px){
  .process-track{grid-template-columns:repeat(5,1fr); gap:0;}
  .process-track::before{
    content:''; position:absolute; top:29px; left:6%; right:6%; height:1px;
    background:repeating-linear-gradient(90deg, var(--line) 0 8px, transparent 8px 16px);
  }
}
.process-step{padding:0 4px; position:relative;}
@media (min-width:900px){ .process-step{padding:0 18px;} }
.step-badge{
  width:52px;height:52px; border-radius:50%; background:var(--ivory); border:1px solid var(--line);
  display:flex; align-items:center; justify-content:center; font-family:var(--font-display); font-weight:700;
  color:var(--teal-deep); margin-bottom:20px; position:relative; z-index:1; box-shadow:var(--shadow-sm);
}
@media (min-width:640px){ .step-badge{width:58px; height:58px; margin-bottom:26px;} }
.process-step h4{font-size:16.5px; margin-bottom:8px;}
@media (min-width:640px){ .process-step h4{font-size:17px; margin-bottom:10px;} }
.process-step p{font-size:14px;}

/* ============================= INDUSTRIES ============================= */
.industry-scroll{display:flex; gap:16px; overflow-x:auto; padding-bottom:14px; scroll-snap-type:x mandatory; -webkit-overflow-scrolling:touch;}
@media (min-width:640px){ .industry-scroll{gap:20px;} }
.industry-scroll::-webkit-scrollbar{height:5px;}
.industry-scroll::-webkit-scrollbar-thumb{background:var(--silver); border-radius:10px;}
.industry-card{
  min-width:190px; scroll-snap-align:start; background:var(--navy-ink); color:var(--ivory);
  border-radius:var(--radius-md); padding:24px 20px; position:relative; overflow:hidden;
  transition:transform .4s var(--ease);
}
@media (min-width:640px){ .industry-card{min-width:230px; padding:30px 24px;} }
.industry-card:nth-child(3n+1){background:var(--teal-deep);}
.industry-card:nth-child(3n+2){background:var(--amber-deep);}
.industry-card:nth-child(3n){background:var(--navy-ink);}
.industry-card:hover, .industry-card:active{transform:translateY(-6px);}
.industry-card .n{font-family:var(--font-display); font-size:12px; color:var(--teal-2); font-weight:600;}
.industry-card h4{color:var(--ivory); font-size:16.5px; margin-top:28px;}
@media (min-width:640px){ .industry-card h4{font-size:18px; margin-top:34px;} }

/* ============================= INDUSTRY CARDS PREMIUM 3D ============================= */
.industry-scroll{ perspective:1000px; }
.industry-card{
  position:relative; overflow:hidden;
  transform-style:preserve-3d;
  will-change:transform;
  transition:transform .45s var(--ease), box-shadow .45s var(--ease);
  border:1px solid rgba(255,255,255,0.08);
}
.industry-card::before{
  content:''; position:absolute; inset:0; z-index:0; pointer-events:none;
  background:linear-gradient(120deg, transparent 30%, rgba(255,255,255,0.16) 48%, transparent 65%);
  transform:translateX(-130%);
  transition:transform .8s var(--ease);
}
.industry-card:hover::before{ transform:translateX(130%); }
.industry-card > *{ position:relative; z-index:1; }
.industry-card:hover, .industry-card:active{
  box-shadow:0 26px 50px -20px rgba(10,17,40,0.5);
}
.industry-card .n{
  display:inline-block; transition:transform .4s var(--ease);
}
.industry-card:hover .n{ transform:translateZ(18px) scale(1.1); }
.industry-card h4{ transition:transform .4s var(--ease); }
.industry-card:hover h4{ transform:translateZ(14px); }

/* ============================= TESTIMONIALS ============================= */
.test-wrap{max-width:820px; margin:0 auto; text-align:center; position:relative;}
.test-slide{display:none;}
.test-slide.active{display:block; animation:fadeUp .6s var(--ease);}
@keyframes fadeUp{from{opacity:0; transform:translateY(14px);} to{opacity:1; transform:translateY(0);}}
.test-quote-mark{font-family:var(--font-display); font-size:48px; color:var(--amber); line-height:1;}
@media (min-width:640px){ .test-quote-mark{font-size:60px;} }
.test-text{font-family:var(--font-display); font-size:clamp(18px,4.6vw,28px); font-weight:500; color:var(--navy-ink); line-height:1.5; margin-top:4px;}
.test-author{margin-top:24px; display:flex; align-items:center; justify-content:center; gap:14px;}
@media (min-width:640px){ .test-author{margin-top:28px;} }
.avatar-fallback{
  width:44px;height:44px;border-radius:50%; background:var(--teal-deep); color:var(--teal-2);
  display:flex;align-items:center;justify-content:center; font-family:var(--font-display); font-weight:700; flex-shrink:0;
}
@media (min-width:640px){ .avatar-fallback{width:48px; height:48px;} }
.author-meta{text-align:left;}
.author-meta h5{font-size:14.5px; margin:0; font-family:var(--font-body); font-weight:700;}
.author-meta span{font-size:12.5px; color:#8F8B80;}
.test-dots{display:flex; justify-content:center; gap:10px; margin-top:30px;}
@media (min-width:640px){ .test-dots{margin-top:36px;} }
.test-dot{width:8px;height:8px;border-radius:50%; background:var(--line); border:none; cursor:pointer; transition:all .3s ease; -webkit-tap-highlight-color:transparent;}
.test-dot.active{background:var(--amber); width:24px; border-radius:6px;}

/* ============================= TESTIMONIALS PREMIUM 3D GLASS ============================= */
.test-wrap{ perspective:1200px; }
.test-card{
  position:relative;
  background:linear-gradient(160deg, rgba(255,255,255,0.55), rgba(255,255,255,0.18));
  border:1px solid rgba(255,255,255,0.5);
  border-radius:var(--radius-lg);
  backdrop-filter:blur(14px);
  box-shadow:var(--shadow-sm);
  padding:40px 28px;
  transform-style:preserve-3d;
  will-change:transform;
  transition:transform .4s var(--ease), box-shadow .4s var(--ease);
}
@media (min-width:640px){ .test-card{padding:52px 48px;} }
.test-card::before{
  content:''; position:absolute; inset:0; border-radius:inherit; pointer-events:none;
  background:radial-gradient(360px circle at var(--tx,50%) var(--ty,20%), rgba(201,162,39,0.14), transparent 60%);
  opacity:0; transition:opacity .4s ease;
}
.test-card:hover::before{ opacity:1; }
.test-card:hover{ box-shadow:0 34px 70px -28px rgba(10,17,40,0.28); }
.test-quote-mark{ position:relative; z-index:1; transition:transform .4s var(--ease); }
.test-card:hover .test-quote-mark{ transform:translateZ(24px) scale(1.08); }
.test-text, .test-author{ position:relative; z-index:1; }

.avatar-fallback{
  transition:transform .4s var(--ease);
  box-shadow:0 8px 18px -8px rgba(14,124,140,0.4);
}
.test-card:hover .avatar-fallback{ transform:translateZ(16px) scale(1.06); }

.test-dot{ transform-style:preserve-3d; }
.test-dot:hover{ transform:scale(1.3); }

/* ============================= FAQ ============================= */
.faq-wrap{max-width:800px; margin:0 auto; perspective:1000px;}
.faq-search{position:relative; margin-bottom:28px;}
@media (min-width:640px){ .faq-search{margin-bottom:36px;} }
.faq-search input{
  width:100%; padding:16px 22px 16px 46px; border-radius:100px; border:1px solid var(--line);
  background:var(--ivory); font-size:14.5px; font-family:inherit; outline:none; transition:border-color .3s ease, box-shadow .3s ease;
}
@media (min-width:640px){ .faq-search input{padding:18px 24px 18px 50px; font-size:15px;} }
.faq-search input:focus{border-color:var(--teal-deep); box-shadow:0 0 0 4px rgba(14,124,140,0.10);}
.faq-search .sicon{position:absolute; left:20px; top:50%; transform:translateY(-50%); color:#8F8B80;}
.faq-item{border-bottom:1px solid var(--line);}
.faq-item[hidden]{display:none;}
.faq-q{
  width:100%; text-align:left; background:none; border:none; padding:18px 2px; cursor:pointer;
  display:flex; justify-content:space-between; align-items:center; gap:16px; font-family:var(--font-display);
  font-size:15.5px; font-weight:600; color:var(--navy-ink); -webkit-tap-highlight-color:transparent;
}
@media (min-width:640px){ .faq-q{padding:22px 4px; font-size:17px;} }
.faq-q .fplus{font-size:18px; color:var(--amber); transition:transform .4s var(--ease); flex-shrink:0;}
.faq-item.open .fplus{transform:rotate(45deg);}
/* .faq-a replaced by .faq-a-panel 3D flip — see faq-flip rules above */

/* ============================= FAQ PREMIUM 3D + GLASS ============================= */
.faq-search input{
  transition:border-color .3s ease, box-shadow .3s ease, background .3s ease, transform .3s var(--ease);
}
.faq-search input:focus{
  transform:translateY(-2px);
  box-shadow:0 0 0 4px rgba(14,124,140,0.10), 0 14px 30px -14px rgba(10,17,40,0.25);
}
.faq-search .sicon{ transition:transform .3s var(--ease), color .3s ease; }
.faq-search input:focus ~ .sicon{ transform:translateY(-50%) scale(1.15); color:var(--teal-deep); }

.faq-item{
  position:relative;
  transition:background .35s ease, border-color .35s ease, box-shadow .35s ease;
  border-radius:14px;
}
.faq-flip{
  position:relative; width:100%;
}
.faq-item.open .faq-flip{ transform:none; }
.faq-a-panel{
  transform-style:preserve-3d;
  transform-origin:top center;
  transform:rotateX(-90deg);
  opacity:0;
  max-height:0; overflow:hidden;
  background:linear-gradient(160deg, rgba(255,255,255,0.75), rgba(255,255,255,0.35));
  border:1px solid rgba(255,255,255,0.5);
  border-radius:12px;
  backdrop-filter:blur(10px);
  box-shadow:0 20px 40px -20px rgba(10,17,40,0.25);
  transition:transform .6s var(--ease), opacity .5s ease, max-height 0s linear .6s;
  margin:0 4px 0 44px;
}
@media (min-width:640px){ .faq-a-panel{ margin:0 8px 0 64px; } }
.faq-item.open .faq-a-panel{
  transform:rotateX(0deg);
  opacity:1;
  max-height:400px;
  transition:transform .6s var(--ease) .1s, opacity .5s ease .15s, max-height 0s linear;
}
.faq-a-panel p{ padding:16px 20px 20px; font-size:14.5px; }
@media (prefers-reduced-motion: reduce){
  .faq-flip, .faq-a-panel{ transition:opacity .3s ease; transform:none !important; }
}
.faq-item:hover{
  background:rgba(255,255,255,0.6);
  box-shadow:var(--shadow-sm);
}
.faq-item.open{
  background:var(--ivory);
  border-color:transparent;
  box-shadow:var(--shadow-sm);
}
.faq-q{ transition:color .3s ease; }
.faq-item:hover .faq-q, .faq-item.open .faq-q{ color:var(--teal-deep); }
.fplus{
  display:inline-flex; align-items:center; justify-content:center;
  width:26px; height:26px; border-radius:50%;
  background:rgba(201,162,39,0.10);
  transition:transform .4s var(--ease), background .3s ease, color .3s ease;
}
.faq-item.open .fplus{ background:var(--teal-deep); color:var(--ivory); }
.faq-a p{
  opacity:0; transform:translateY(-6px);
  transition:opacity .4s var(--ease) .1s, transform .4s var(--ease) .1s;
}
.faq-item.open .faq-a p{ opacity:1; transform:translateY(0); }

/* ============================= CONTACT ============================= */
.contact-panel{
  background:linear-gradient(160deg,var(--navy-ink),var(--teal-deep)); border-radius:22px; padding:32px 22px;
  display:grid; grid-template-columns:1fr; gap:36px; color:var(--ivory);
}
@media (min-width:640px){ .contact-panel{border-radius:28px; padding:44px 34px;} }
@media (min-width:900px){ .contact-panel{grid-template-columns:.9fr 1.1fr; gap:50px; padding:64px;} }
.contact-panel h2{color:var(--ivory);}
.contact-list{margin-top:26px; display:grid; gap:18px;}
@media (min-width:640px){ .contact-list{margin-top:30px; gap:22px;} }
.c-row{display:flex; gap:14px; align-items:flex-start;}
.c-icon{width:38px;height:38px;border-radius:10px; background:rgba(255,255,255,0.1); display:flex; align-items:center; justify-content:center; flex-shrink:0; color:var(--teal-2);}
.c-row h5{font-size:13px; margin:0 0 4px; color:rgba(250,247,242,0.6); font-weight:500; letter-spacing:.02em;}
.c-row p{color:var(--ivory); font-size:15px;}

.form-grid{display:grid; gap:16px;}
.field{position:relative;}
.field input, .field textarea{
  width:100%; background:rgba(255,255,255,0.07); border:1px solid rgba(255,255,255,0.18);
  border-radius:12px; padding:18px 16px 8px; color:var(--ivory); font-family:inherit; font-size:15px; outline:none;
  transition:border-color .3s ease, background .3s ease;
}
.field textarea{resize:vertical; min-height:100px;}
.field label{
  position:absolute; left:17px; top:17px; color:rgba(250,247,242,0.5); font-size:15px; pointer-events:none;
  transition:all .25s var(--ease);
}
.field input:focus, .field textarea:focus{border-color:var(--amber); background:rgba(255,255,255,0.1);}
.field input:focus + label, .field input:not(:placeholder-shown) + label,
.field textarea:focus + label, .field textarea:not(:placeholder-shown) + label{
  top:7px; font-size:11px; color:var(--teal-2);
}
.two-col{display:grid; grid-template-columns:1fr; gap:16px;}
@media (min-width:480px){ .two-col{grid-template-columns:1fr 1fr; gap:18px;} }
.form-status{
  border-radius:12px; padding:13px 16px; font-size:13.5px; line-height:1.55;
  font-family:var(--font-body);
}
.form-status.success{
  background:rgba(74,222,128,0.14); border:1px solid rgba(74,222,128,0.4); color:#4ADE80;
}
.form-status.error{
  background:rgba(220,38,38,0.14); border:1px solid rgba(248,113,113,0.4); color:#FCA5A5;
}

/* ============================= CONTACT PANEL PREMIUM 3D GLASS ============================= */
.contact-panel{
  position:relative; overflow:hidden;
  border:1px solid rgba(255,255,255,0.12);
  box-shadow:var(--shadow-lg);
}
.contact-panel::before{
  content:''; position:absolute; inset:0; z-index:0; pointer-events:none;
  background:radial-gradient(460px circle at var(--px,20%) var(--py,20%), rgba(232,199,102,0.12), transparent 60%);
  opacity:0; transition:opacity .5s ease;
}
.contact-panel:hover::before{ opacity:1; }
.contact-panel > *{ position:relative; z-index:1; }

.c-icon{
  transition:transform .4s var(--ease), background .35s ease, color .35s ease;
}
.c-row:hover .c-icon{
  transform:translateY(-3px) rotate(-6deg) scale(1.08);
  background:rgba(232,199,102,0.18); color:var(--amber-2);
}

.field input, .field textarea{
  position:relative;
}
.field::after{
  content:''; position:absolute; inset:0; border-radius:12px; pointer-events:none;
  box-shadow:0 0 0 0 rgba(232,199,102,0);
  transition:box-shadow .35s ease;
}
.field:focus-within::after{ box-shadow:0 0 0 4px rgba(232,199,102,0.16); }
.field:focus-within input, .field:focus-within textarea{ background:rgba(255,255,255,0.13); }

#contactForm button[type="submit"]{ position:relative; overflow:hidden; will-change:transform; }
.form-btn-shine{
  position:absolute; top:0; left:-60%; width:40%; height:100%;
  background:linear-gradient(120deg, transparent, rgba(255,255,255,0.55), transparent);
  transform:skewX(-20deg); pointer-events:none;
}
#contactForm button[type="submit"]:hover .form-btn-shine{ animation:shineSweep 1s var(--ease); }
@keyframes shineSweep{ from{ left:-60%; } to{ left:130%; } }

/* ============================= FOOTER — PREMIUM REDESIGN ============================= */
footer{
  position:relative; overflow:hidden; color:rgba(250,247,242,0.72);
  padding:0 0 0;
  background:
    radial-gradient(900px 480px at 15% 0%, rgba(232,199,102,0.08), transparent 60%),
    radial-gradient(800px 500px at 100% 100%, rgba(143,160,232,0.10), transparent 60%),
    linear-gradient(190deg, var(--navy-ink) 0%, var(--teal-deep) 55%, #0B1330 100%);
}
footer::after{
  content:''; position:absolute; inset:0; pointer-events:none; opacity:.45; z-index:0;
  background:
    linear-gradient(rgba(255,255,255,0.03) 1px, transparent 1px),
    linear-gradient(90deg, rgba(255,255,255,0.03) 1px, transparent 1px);
  background-size:52px 52px;
  mask-image:radial-gradient(ellipse 90% 60% at 50% 0%, #000 30%, transparent 100%);
  -webkit-mask-image:radial-gradient(ellipse 90% 60% at 50% 0%, #000 30%, transparent 100%);
}
footer::before{
  content:''; position:absolute; inset:0; z-index:1; pointer-events:none;
  background:radial-gradient(520px circle at var(--fx,50%) var(--fy,0%), rgba(232,199,102,0.10), transparent 65%);
  opacity:0; transition:opacity .5s ease;
}
footer:hover::before{ opacity:1; }
footer .wrap{ position:relative; z-index:2; }
.footer-orb{position:absolute; border-radius:50%; filter:blur(80px); opacity:.20; pointer-events:none; z-index:0;}
.footer-orb-1{width:320px;height:320px; background:var(--amber-2); top:-100px; left:-60px; animation:orbFloat 20s ease-in-out infinite;}
.footer-orb-2{width:260px;height:260px; background:var(--teal-2); bottom:-80px; right:-40px; animation:orbFloat 24s ease-in-out infinite; animation-delay:3s;}
@media (prefers-reduced-motion: reduce){ .footer-orb{animation:none;} }

@media (max-width:900px){ footer{padding-bottom:calc(88px + env(safe-area-inset-bottom));} }

.footer-cta{
  display:flex; flex-direction:column; gap:20px; align-items:flex-start;
  padding:44px 0 40px; border-bottom:1px solid rgba(255,255,255,0.10);
}
@media (min-width:768px){
  .footer-cta{ flex-direction:row; align-items:center; justify-content:space-between; padding:56px 0 48px; }
}
.footer-cta-text h3{ color:var(--ivory); font-size:clamp(20px,4vw,30px); margin-top:10px; line-height:1.25; max-width:560px; }
.footer-cta .btn{ flex-shrink:0; }

.footer-grid{display:grid; grid-template-columns:1fr; gap:36px; padding:48px 0 40px; border-bottom:1px solid rgba(255,255,255,0.08);}
@media (min-width:640px){ .footer-grid{grid-template-columns:1fr 1fr; row-gap:40px;} }
@media (min-width:1024px){ .footer-grid{grid-template-columns:1.4fr 1fr 1fr 1.2fr; gap:50px; padding:60px 0 52px;} }

.footer-brand{display:flex; flex-direction:column; align-items:flex-start;}
.footer-logo{height:84px; width:auto; max-width:170px; object-fit:contain; margin-bottom:4px; filter:brightness(0) invert(1); transition:transform .4s var(--ease);}
.footer-brand:hover .footer-logo{ transform:translateY(-3px) scale(1.04); }
.footer-brand p{color:rgba(250,247,242,0.55); font-size:14px; margin-top:14px; max-width:280px;}
.footer-est{
  display:inline-flex; align-items:center; gap:8px;
  font-family:var(--font-eyebrow); font-size:11px; letter-spacing:.06em; text-transform:uppercase; color:var(--amber-2);
  margin-top:14px; padding:7px 14px; border-radius:100px;
  background:rgba(232,199,102,0.08); border:1px solid rgba(232,199,102,0.25);
}
.footer-est::before{ content:'◆'; font-size:7px; }

.footer-mini-contact{ display:grid; gap:10px; margin-top:20px; }
.footer-mini-contact a{
  display:flex; align-items:center; gap:10px; color:rgba(250,247,242,0.75); text-decoration:none; font-size:13.5px;
  transition:color .3s ease, transform .3s var(--ease);
}
.footer-mini-contact a:hover{ color:var(--teal-2); transform:translateX(3px); }
.fmc-icon{
  width:28px; height:28px; border-radius:9px; flex-shrink:0;
  display:flex; align-items:center; justify-content:center; font-size:12px;
  background:rgba(255,255,255,0.07); border:1px solid rgba(255,255,255,0.14); color:var(--teal-2);
}

.footer-col h5{color:var(--ivory); font-size:13px; letter-spacing:.06em; text-transform:uppercase; margin-bottom:20px; font-family:var(--font-eyebrow); font-weight:600; position:relative; padding-bottom:12px;}
.footer-col h5::after{ content:''; position:absolute; left:0; bottom:0; width:26px; height:2px; background:linear-gradient(90deg,var(--amber),var(--amber-2)); border-radius:2px; }
.footer-col ul{display:grid; gap:12px; list-style:none;}
.footer-col a{color:rgba(250,247,242,0.62); text-decoration:none; font-size:14px; position:relative; display:inline-flex; align-items:center; gap:8px; transition:color .3s ease, transform .25s var(--ease);}
.footer-col a::before{ content:'→'; font-size:11px; color:var(--amber-2); opacity:0; transform:translateX(-6px); transition:opacity .25s ease, transform .25s var(--ease); }
.footer-col a:hover{ color:var(--ivory); transform:translateX(4px); }
.footer-col a:hover::before{ opacity:1; transform:translateX(0); }

.social-row{display:flex; gap:12px; margin-top:20px;}
.social-row a{
  width:40px;height:40px;border-radius:12px;
  background:rgba(255,255,255,0.06); border:1px solid rgba(255,255,255,0.14);
  display:flex;align-items:center;justify-content:center; font-size:14px; color:var(--ivory);
  transform-style:preserve-3d; will-change:transform;
  transition:transform .55s var(--ease), background .3s ease, border-color .3s ease, box-shadow .3s ease;
}
.social-row a:hover{
  background:linear-gradient(135deg,var(--amber-2),var(--amber)); border-color:transparent; color:#fff;
  transform:translateY(-4px) rotateY(360deg);
  box-shadow:0 14px 26px -12px rgba(201,162,39,0.6);
}

.sub-form{display:flex; gap:0; margin-top:16px; border:1px solid rgba(255,255,255,0.16); border-radius:100px; overflow:hidden; padding:4px; background:rgba(255,255,255,0.04); transition:box-shadow .3s ease, border-color .3s ease;}
.sub-form:focus-within{ box-shadow:0 0 0 4px rgba(232,199,102,0.14); border-color:rgba(232,199,102,0.45); }
.sub-form input{flex:1; min-width:0; background:none; border:none; padding:11px 16px; color:var(--ivory); outline:none; font-family:inherit; font-size:13.5px;}
.sub-form button{
  background:linear-gradient(135deg,var(--amber-2),var(--amber)); border:none; color:#fff; font-weight:700;
  padding:11px 20px; border-radius:100px; cursor:pointer; font-size:12.5px; flex-shrink:0;
  transition:transform .3s var(--ease), box-shadow .3s ease;
}
.sub-form button:hover{ transform:translateY(-2px) scale(1.04); box-shadow:0 10px 22px -10px rgba(201,162,39,0.65); }

.footer-bottom{display:flex; justify-content:space-between; align-items:center; padding:24px 0; font-size:12.5px; color:rgba(250,247,242,0.42); flex-wrap:wrap; gap:10px; text-align:center;}
@media (min-width:768px){ .footer-bottom{padding:28px 0; font-size:13px;} }

.back-to-top{ transform-style:preserve-3d; }
.back-to-top:hover{ transform:translateY(-4px) rotateX(10deg); }

/* Center footer content on mobile */
@media (max-width:639px){
  .footer-cta{ text-align:center; align-items:center; }
  .footer-cta-text{ margin-left:auto; margin-right:auto; }
  .footer-grid{ text-align:center; }
  .footer-brand{ align-items:center; }
  .footer-brand p{ margin-left:auto; margin-right:auto; }
  .footer-mini-contact{ justify-items:center; }
  .footer-mini-contact a{ justify-content:center; }
  .footer-col ul{ justify-items:center; }
  .footer-col a::before{ display:none; }
  .footer-col h5{ display:inline-block;  margin-left:auto; margin-right:auto; }
  .footer-col h5::after{ left:50%; transform:translateX(-50%); }
  .social-row{ justify-content:center; }
  .sub-form{ max-width:320px; margin-left:auto; margin-right:auto; }
  .footer-bottom{ justify-content:center; text-align:center; }
}

/* Back to top */
.back-to-top{
  position:fixed; right:18px; bottom:80px; width:46px; height:46px; border-radius:50%;
  background:rgba(27,31,39,0.85); border:1px solid rgba(255,255,255,0.14); backdrop-filter:blur(10px);
  color:var(--teal-2); display:flex; align-items:center; justify-content:center; cursor:pointer; z-index:900;
  opacity:0; visibility:hidden; transform:translateY(16px);
  transition:opacity .4s var(--ease), transform .4s var(--ease), background .3s ease, border-color .3s ease;
  box-shadow:var(--shadow-sm); -webkit-tap-highlight-color:transparent;
}
@media (min-width:901px){ .back-to-top{right:28px; bottom:28px; width:52px; height:52px;} }
.back-to-top.show{opacity:1; visibility:visible; transform:translateY(0);}
.back-to-top:hover{background:linear-gradient(135deg,var(--amber-2),var(--amber)); color:#fff; border-color:var(--amber); transform:translateY(-4px);}
.back-to-top svg{transition:transform .3s var(--ease);}
.back-to-top:hover svg{transform:translateY(-2px);}

/* ============================= SCROLL PROGRESS ============================= */
.scroll-progress{
  position:fixed; top:0; left:0; height:3px; width:0%; z-index:1200;
  background:linear-gradient(90deg,var(--amber),var(--amber-2),var(--teal-2));
  box-shadow:0 0 14px rgba(232,199,102,0.55);
  transition:width .12s linear;
}
.route-transition-bar{
  position:fixed; top:0; left:0; height:3px; width:0%; z-index:1300;
  background:linear-gradient(90deg,var(--teal-2),var(--amber-2));
  box-shadow:0 0 12px rgba(143,160,232,0.7);
  opacity:0; transition:width .3s ease, opacity .2s ease;
}
.route-transition-bar.active{ opacity:1; }

.section-fade-target{
  transition:opacity .28s ease, filter .28s ease;
}
.section-fade-target.fading{
  opacity:.35; filter:blur(2px);
}

/* ============================= PRODUCTS & SOLUTIONS ============================= */
.products-band{
  position:relative; color:var(--ivory); overflow:hidden;
  background:
    radial-gradient(900px 480px at 12% -10%, rgba(232,199,102,0.10), transparent 60%),
    radial-gradient(800px 500px at 100% 110%, rgba(143,160,232,0.14), transparent 60%),
    linear-gradient(200deg, #0F4436 0%, var(--navy-ink) 45%, #0B1330 100%);
}
.products-band::before{
  content:''; position:absolute; inset:0; pointer-events:none; opacity:.5;
  background:
    linear-gradient(rgba(255,255,255,0.035) 1px, transparent 1px),
    linear-gradient(90deg, rgba(255,255,255,0.035) 1px, transparent 1px);
  background-size:56px 56px;
  mask-image:radial-gradient(ellipse 90% 70% at 50% 40%, #000 30%, transparent 100%);
  -webkit-mask-image:radial-gradient(ellipse 90% 70% at 50% 40%, #000 30%, transparent 100%);
}
.products-band .orb{position:absolute; border-radius:50%; filter:blur(80px); opacity:.22; pointer-events:none;}
.products-band .p-orb-1{width:340px;height:340px;background:var(--amber-2);top:-90px;left:-60px;animation:orbFloat 19s ease-in-out infinite;}
.products-band .p-orb-2{width:260px;height:260px;background:var(--teal-2);bottom:-70px;right:-40px;animation:orbFloat 24s ease-in-out infinite;animation-delay:3s;}
.products-band .section-head h2{color:var(--ivory);}
.products-band .section-head p{color:rgba(250,247,242,0.68);}
.products-band .eyebrow{color:var(--amber-2);}
.products-band .eyebrow::before{background:var(--amber-2);}

.prod-grid{
  position:relative; z-index:1;
  display:grid; grid-template-columns:1fr; gap:24px;
  perspective:1600px;
}
.prod-grid.deck-in .prod-card:nth-child(1){ animation:deckFromLeft .8s var(--ease) forwards; }
.prod-grid.deck-in .prod-card:nth-child(2){ animation:deckFromBelow .8s var(--ease) forwards; animation-delay:.12s; }
.prod-grid.deck-in .prod-card:nth-child(3){ animation:deckFromRight .8s var(--ease) forwards; animation-delay:.24s; }
.prod-grid:not(.deck-in) .prod-card{ opacity:0; }


/* ============================= PRODUCTS SECTION — EXTRA 3D FX ============================= */
.products-band{ perspective:1800px; }
.products-band-spotlight{
  position:absolute; inset:0; z-index:0; pointer-events:none;
  background:radial-gradient(560px circle at var(--psx,50%) var(--psy,30%), rgba(232,199,102,0.10), transparent 60%);
  opacity:0; transition:opacity .5s ease;
}
.products-band-spotlight.on{ opacity:1; }

.prod-particles{ position:absolute; inset:0; z-index:0; pointer-events:none; overflow:hidden; }
.prod-particle{
  position:absolute; border-radius:50%; filter:blur(.5px);
  animation-name:prodParticleDrift; animation-timing-function:ease-in-out; animation-iteration-count:infinite;
}
@keyframes prodParticleDrift{
  0%{ transform:translate3d(0,0,0) scale(1); opacity:.25; }
  50%{ transform:translate3d(14px,-30px,0) scale(1.5); opacity:.75; }
  100%{ transform:translate3d(0,0,0) scale(1); opacity:.25; }
}
@media (prefers-reduced-motion: reduce){ .prod-particle{ animation:none; } }

/* Deeper card lift + longer shadow throw on hover */
.prod-card{ transition:transform .5s var(--ease), box-shadow .5s var(--ease), border-color .4s ease; }
.prod-card:hover{ transform:translateY(-10px) scale(1.015); }

/* Pulsing glow ring behind badges */
.prod-badge{ position:relative; }
.prod-badge::after{
  content:''; position:absolute; inset:-6px; border-radius:100px; z-index:-1;
  border:1px solid currentColor; opacity:0; animation:badgePulse 2.4s ease-out infinite;
}
.prod-badge.gold::after{ border-color:var(--amber-2); }
.prod-badge.free::after{ border-color:#4ADE80; }
.prod-badge.soon-badge::after{ border-color:var(--teal-2); }
@keyframes badgePulse{
  0%{ opacity:.55; transform:scale(0.9); }
  100%{ opacity:0; transform:scale(1.35); }
}
@media (prefers-reduced-motion: reduce){ .prod-badge::after{ animation:none; display:none; } }

/* Premium card: stronger pulsing outer glow */
.prod-card.premium{ position:relative; }
.prod-card.premium::after{
  content:''; position:absolute; inset:-14px; z-index:-1; border-radius:32px; pointer-events:none;
  background:radial-gradient(closest-side, rgba(201,162,39,0.30), transparent 75%);
  opacity:.5; animation:premiumBreathe 3.6s ease-in-out infinite;
}
@keyframes premiumBreathe{
  0%,100%{ opacity:.35; transform:scale(0.98); }
  50%{ opacity:.7; transform:scale(1.04); }
}
@media (prefers-reduced-motion: reduce){ .prod-card.premium::after{ animation:none; } }

/* Icon: subtle continuous 3D wobble on top of existing float */
.prod-icon{ animation:iconFloat 4.5s ease-in-out infinite, iconWobble 7s ease-in-out infinite; }
@keyframes iconWobble{
  0%,100%{ transform:translateZ(30px) rotateZ(0deg); }
  50%{ transform:translateZ(30px) rotateZ(-4deg); }
}

/* Card title: gradient sweep underline reveal on hover */
.prod-card h3{ position:relative; display:inline-block; }
.prod-card h3::after{
  content:''; position:absolute; left:0; bottom:-4px; height:2px; width:0%;
  background:linear-gradient(90deg,var(--amber-2),var(--teal-2));
  transition:width .5s var(--ease);
}
.prod-card:hover h3::after{ width:100%; }

/* Section heading: extra depth on hover of whole grid */
.products-band .section-head h2{ text-shadow:0 0 30px rgba(232,199,102,0.0); transition:text-shadow .6s ease; }
.products-band:hover .section-head h2{ text-shadow:0 0 30px rgba(232,199,102,0.18); }

/* Feature list items: slide-in stagger on card hover */
.prod-feats li{ transition:transform .35s var(--ease), color .3s ease; }
.prod-card:hover .prod-feats li:nth-child(1){ transition-delay:.02s; }
.prod-card:hover .prod-feats li:nth-child(2){ transition-delay:.06s; }
.prod-card:hover .prod-feats li:nth-child(3){ transition-delay:.10s; }
.prod-card:hover .prod-feats li:nth-child(4){ transition-delay:.14s; }
.prod-card:hover .prod-feats li:nth-child(5){ transition-delay:.18s; }
.prod-card:hover .prod-feats li:nth-child(6){ transition-delay:.22s; }
.prod-card:hover .prod-feats li{ transform:translateX(4px) translateZ(6px); }

@keyframes deckFromLeft{
  from{ opacity:0; transform:translateX(-70px) rotateY(35deg) rotateZ(-4deg) scale(.9); }
  to{ opacity:1; transform:translateX(0) rotateY(0) rotateZ(0) scale(1); }
}
@keyframes deckFromRight{
  from{ opacity:0; transform:translateX(70px) rotateY(-35deg) rotateZ(4deg) scale(.9); }
  to{ opacity:1; transform:translateX(0) rotateY(0) rotateZ(0) scale(1); }
}
@keyframes deckFromBelow{
  from{ opacity:0; transform:translateY(60px) rotateX(-25deg) scale(.9); }
  to{ opacity:1; transform:translateY(0) rotateX(0) scale(1); }
}
@media (prefers-reduced-motion: reduce){
  .prod-grid:not(.deck-in) .prod-card{ opacity:1; }
  .prod-grid.deck-in .prod-card{ animation:none; }
}
@media (min-width:1024px){ .prod-grid{grid-template-columns:repeat(3,1fr); gap:26px; align-items:stretch;} }

.prod-card{
  position:relative; display:flex; flex-direction:column;
  border-radius:24px; padding:30px 24px 26px;
  background:linear-gradient(165deg, rgba(255,255,255,0.075), rgba(255,255,255,0.025));
  border:1px solid rgba(255,255,255,0.13);
  backdrop-filter:blur(16px);
  transform-style:preserve-3d; will-change:transform;
  transition:transform .4s var(--ease), box-shadow .4s var(--ease), border-color .35s ease;
  overflow:hidden; -webkit-tap-highlight-color:transparent;
  --mx:50%; --my:40%;
}
@media (min-width:640px){ .prod-card{padding:34px 28px 30px;} }
.prod-card:hover{border-color:rgba(232,199,102,0.35); box-shadow:0 44px 90px -34px rgba(0,0,0,0.65);}
.prod-glare{
  content:''; position:absolute; inset:0; pointer-events:none; z-index:0; opacity:0;
  background:radial-gradient(420px circle at var(--mx) var(--my), rgba(255,255,255,0.10), transparent 55%);
  transition:opacity .4s ease;
}
.prod-card:hover .prod-glare{opacity:1;}
.prod-card > *:not(.prod-glare){position:relative; z-index:1;}

/* Animated conic border for the Premium card */
@property --ang{syntax:'<angle>'; initial-value:0deg; inherits:false;}
.prod-card.premium{background:linear-gradient(165deg, rgba(201,162,39,0.14), rgba(255,255,255,0.03));}
.prod-card.premium::before{
  content:''; position:absolute; inset:-1px; border-radius:inherit; padding:1.6px; z-index:0;
  background:conic-gradient(from var(--ang), var(--amber-2), transparent 25%, var(--amber) 50%, transparent 75%, var(--amber-2));
  -webkit-mask:linear-gradient(#000 0 0) content-box, linear-gradient(#000 0 0);
  -webkit-mask-composite:xor; mask-composite:exclude;
  animation:spinBorder 6s linear infinite; pointer-events:none;
}
@keyframes spinBorder{to{--ang:360deg;}}
@media (prefers-reduced-motion: reduce){ .prod-card.premium::before{animation:none;} }

/* Coming-soon card gets a cool futuristic tint */
.prod-card.soon{background:linear-gradient(165deg, rgba(143,160,232,0.13), rgba(255,255,255,0.02)); border-color:rgba(143,160,232,0.30);}
.prod-card.soon:hover{border-color:rgba(143,160,232,0.55);}

.prod-top{display:flex; align-items:center; justify-content:space-between; gap:12px; margin-bottom:20px;}
.prod-icon{
  width:54px;height:54px; border-radius:16px; flex-shrink:0;
  display:flex; align-items:center; justify-content:center; font-size:22px;
  background:linear-gradient(145deg, rgba(255,255,255,0.14), rgba(255,255,255,0.04));
  border:1px solid rgba(255,255,255,0.16); color:var(--amber-2);
  box-shadow:inset 0 1px 0 rgba(255,255,255,0.18), 0 14px 26px -14px rgba(0,0,0,0.6);
  transform:translateZ(30px);
  transition:transform .45s var(--ease);
}
.prod-card:hover .prod-icon{transform:translateZ(46px) rotate(-6deg) scale(1.06);}
.prod-card.soon .prod-icon{color:var(--teal-2);}

.prod-badge{
  font-family:var(--font-eyebrow); font-size:10.5px; font-weight:700; letter-spacing:.09em; text-transform:uppercase;
  padding:7px 13px; border-radius:100px; white-space:nowrap;
}
.prod-badge.free{background:rgba(74,222,128,0.14); color:#4ADE80; border:1px solid rgba(74,222,128,0.35);}
.prod-badge.gold{
  background:linear-gradient(135deg,var(--amber-2),var(--amber)); color:#1C1400;
  box-shadow:0 8px 20px -8px rgba(201,162,39,0.7);
}
.prod-badge.soon-badge{
  background:rgba(143,160,232,0.14); color:var(--teal-2); border:1px solid rgba(143,160,232,0.4);
  display:inline-flex; align-items:center; gap:7px;
}
.prod-badge.soon-badge::before{
  content:''; width:7px;height:7px;border-radius:50%; background:var(--teal-2);
  box-shadow:0 0 0 4px rgba(143,160,232,0.18); animation:pulse 2s infinite;
}

.prod-card h3{color:var(--ivory); font-size:22px; letter-spacing:-.01em; line-height:1.2;}
@media (min-width:640px){ .prod-card h3{font-size:24px;} }
.prod-card h3 em{
  font-style:normal;
  background:linear-gradient(120deg,var(--amber-2),var(--amber));
  -webkit-background-clip:text; background-clip:text; color:transparent;
}
.prod-card.soon h3 em{background:linear-gradient(120deg,var(--teal-2),#B8C4F5); -webkit-background-clip:text; background-clip:text;}
.prod-sub{font-family:var(--font-eyebrow); font-size:11.5px; font-weight:600; letter-spacing:.06em; text-transform:uppercase; color:rgba(250,247,242,0.5); margin-top:7px;}
.prod-desc{color:rgba(250,247,242,0.74); font-size:14.5px; margin-top:14px;}

.prod-feats{list-style:none; margin-top:20px; display:grid; gap:10px;}
.prod-feats li{
  display:flex; gap:10px; align-items:flex-start;
  color:rgba(250,247,242,0.82); font-size:13.5px; line-height:1.5;
}
.prod-feats li::before{
  content:'✓'; flex-shrink:0; width:18px;height:18px; border-radius:50%;
  display:inline-flex; align-items:center; justify-content:center; margin-top:1px;
  background:rgba(232,199,102,0.16); color:var(--amber-2); font-size:10px; font-weight:700;
}
.prod-card.soon .prod-feats li::before{background:rgba(143,160,232,0.16); color:var(--teal-2);}

.prod-chips{display:flex; flex-wrap:wrap; gap:8px; margin-top:18px;}
.prod-chip{
  font-size:11.5px; padding:7px 12px; border-radius:100px;
  background:rgba(255,255,255,0.06); border:1px solid rgba(255,255,255,0.13);
  color:rgba(250,247,242,0.75); transition:all .3s ease;
}
.prod-chip:hover{background:rgba(143,160,232,0.16); border-color:rgba(143,160,232,0.45); color:var(--ivory); transform:translateY(-2px);}

.prod-actions{display:flex; gap:12px; margin-top:auto; padding-top:26px; flex-wrap:wrap;}
.prod-actions .btn{flex:1 1 auto; padding:13px 20px; font-size:13.5px;}
.btn-glass{
  background:rgba(255,255,255,0.07); color:var(--ivory); border-color:rgba(255,255,255,0.22);
}
.btn-glass:hover{background:rgba(255,255,255,0.14); border-color:rgba(255,255,255,0.45); transform:translateY(-2px);}
.btn-soon{
  background:rgba(143,160,232,0.12); color:var(--teal-2); border-color:rgba(143,160,232,0.35); cursor:default;
}

.prod-foot-note{
  position:relative; z-index:1; text-align:center; margin-top:34px;
  color:rgba(250,247,242,0.5); font-size:13px;
}
.prod-foot-note a{color:var(--amber-2); text-decoration:none; font-weight:600;}
.prod-foot-note a:hover{text-decoration:underline;}

/* ============================= PRODUCT CARDS PREMIUM 3D ENHANCEMENTS ============================= */
@keyframes shineSweep{ from{ left:-60%; } to{ left:130%; } }

.prod-icon{ animation:iconFloat 4.5s ease-in-out infinite; }
@keyframes iconFloat{
  0%,100%{ transform:translateZ(30px) translateY(0); }
  50%{ transform:translateZ(30px) translateY(-5px); }
}
.prod-card:hover .prod-icon{ animation-play-state:paused; }
@media (prefers-reduced-motion: reduce){ .prod-icon{ animation:none; } }

.prod-shine{
  position:absolute; inset:0; z-index:0; pointer-events:none; border-radius:inherit;
  background:linear-gradient(115deg, transparent 30%, rgba(255,255,255,0.10) 45%, transparent 60%);
  transform:translateX(-120%);
  transition:transform .9s var(--ease);
}
.prod-card:hover .prod-shine{ transform:translateX(120%); }

.prod-badge{ transition:transform .35s var(--ease), box-shadow .35s var(--ease); }
.prod-card:hover .prod-badge{ transform:translateZ(20px) scale(1.05); }

.prod-actions .btn{ position:relative; overflow:hidden; }
.prod-btn-shine{
  position:absolute; top:0; left:-60%; width:40%; height:100%;
  background:linear-gradient(120deg, transparent, rgba(255,255,255,0.5), transparent);
  transform:skewX(-20deg); pointer-events:none;
}
.prod-actions .btn:hover .prod-btn-shine{ animation:shineSweep 1s var(--ease); }

.prod-feats li{ transition:transform .3s var(--ease); }
.prod-card:hover .prod-feats li{ transform:translateZ(6px); }

.prod-chip{ transform-style:preserve-3d; }

/* Scroll reveal — 3D perspective rise */
.reveal{
  opacity:0; transform:perspective(1000px) translateY(34px) rotateX(8deg) scale(.97);
  filter:blur(6px);
  transform-origin:bottom center;
  transition:opacity .9s var(--ease), transform .9s var(--ease);
  will-change:transform, opacity;
}
.reveal.in{opacity:1; transform:perspective(1000px) translateY(0) rotateX(0deg) scale(1); filter:blur(0);}
.stagger.in > *{transition-delay:calc(var(--i,0) * 70ms);}

/* 3D card-flip entrance for grids that opt in */
.flip-in-grid{ perspective:1400px; }
.flip-in-grid > *{
  opacity:0; transform:rotateY(-70deg) translateZ(40px); transform-origin:left center;
  transition:opacity .8s var(--ease), transform .8s var(--ease);
  transition-delay:calc(var(--i,0) * 110ms);
}
.flip-in-grid.in > *{ opacity:1; transform:rotateY(0deg) translateZ(0); }

/* focus visibility */
a:focus-visible, button:focus-visible, input:focus-visible, textarea:focus-visible{
  outline:2px solid var(--amber); outline-offset:3px;
}

.brand-logo {
    display: block;
    width: auto;
    height: auto;
    max-width: 100%;
    max-height: clamp(42px, 6vw, 90px);
    object-fit: contain;
    transition: transform 0.3s ease;
    user-select: none;
    border-radius: 10px;
}

.brand-logo:hover {
    transform: scale(1.03);
}

/* Tablet */
@media (max-width: 992px) {
    .brand-logo {
        max-height: 52px;
    }
}

/* Mobile */
@media (max-width: 768px) {
    .brand-logo {
        max-height: 46px;
    }
}

/* Small Phones */
@media (max-width: 480px) {
    .brand-logo {
        max-height: 40px;
    }
}

.contact-list a {
    color: inherit;
    text-decoration: none;
    transition: color 0.25s ease;
}

.contact-list a:hover {
    color: var(--primary);
    text-decoration: underline;
}

.contact-list a:focus-visible {
    outline: 2px solid var(--primary);
    outline-offset: 2px;
}

/* ============================= ANCHOR OFFSET (fixed navbar) ============================= */
section[id], header[id]{ scroll-margin-top:88px; }
@media (min-width:901px){ section[id], header[id]{ scroll-margin-top:96px; } }

/* ============================= PAGE-LOAD PRELOADER ============================= */
.preloader{
  position:fixed; inset:0; z-index:3000;
  display:flex; flex-direction:column; align-items:center; justify-content:center; gap:16px;
  background:linear-gradient(160deg, var(--navy-ink) 0%, var(--teal-deep) 65%, #0F4436 100%);
  transition:opacity .6s var(--ease), visibility .6s var(--ease);
}
.preloader.done{ opacity:0; visibility:hidden; pointer-events:none; }
.preloader-mark{ animation:plSpin 1.1s linear infinite; }
@keyframes plSpin{ to{ transform:rotate(360deg); } }
.pl-arc{ stroke-dasharray:80; stroke-dashoffset:60; transform-origin:center; }
.preloader-label{
  font-family:var(--font-eyebrow); font-size:11.5px; letter-spacing:.14em; text-transform:uppercase;
  color:rgba(250,247,242,0.55);
}
@media (prefers-reduced-motion: reduce){
  .preloader{ transition:none; }
  .preloader-mark{ animation:none; }
}

/* ============================= HERO LOAD-IN SEQUENCE ============================= */
.load-in{
  opacity:0; transform:translateY(16px);
  animation:loadInRise .7s var(--ease) forwards;
  animation-delay:calc(var(--d,0) * 90ms + .15s);
}
@keyframes loadInRise{ to{ opacity:1; transform:translateY(0); } }
@media (prefers-reduced-motion: reduce){
  .load-in{ opacity:1; transform:none; animation:none; }
}

/* ============================= SKELETON LOADERS ============================= */
.ledger-skeleton{
  height:48px; border-radius:12px;
  background:linear-gradient(100deg, rgba(255,255,255,0.05) 20%, rgba(255,255,255,0.12) 40%, rgba(255,255,255,0.05) 60%);
  background-size:200% 100%;
  animation:skeletonSheen 1.5s ease-in-out infinite;
}
@keyframes skeletonSheen{ from{ background-position:120% 0; } to{ background-position:-20% 0; } }
@media (prefers-reduced-motion: reduce){ .ledger-skeleton{ animation:none; opacity:.5; } }

/* ============================= SCROLL PARALLAX ============================= */
[data-parallax]{ will-change:transform; }
[data-depth]{ will-change:transform; }

/* ============================= MULTI-PLANE DEPTH PARALLAX ============================= */
[data-plane]{
  will-change:transform;
  transition:transform .1s linear;
}
.depth-stage{ perspective:1200px; }


/* ============================= FORM VALIDATION MICRO-INTERACTIONS ============================= */
.field-error{
  display:block; max-height:0; overflow:hidden; opacity:0;
  color:#FCA5A5; font-size:12px; margin-top:0; padding-left:4px;
  transition:max-height .3s var(--ease), opacity .3s var(--ease), margin-top .3s var(--ease);
}
.field.invalid input, .field.invalid textarea{
  border-color:#F87171; background:rgba(248,113,113,0.08);
}
.field.invalid .field-error{ max-height:24px; opacity:1; margin-top:6px; }
.field.invalid{ animation:fieldShake .4s var(--ease); }
@keyframes fieldShake{
  0%,100%{ transform:translateX(0); }
  20%{ transform:translateX(-5px); }
  40%{ transform:translateX(4px); }
  60%{ transform:translateX(-3px); }
  80%{ transform:translateX(2px); }
}
.field.valid input, .field.valid textarea{ border-color:rgba(74,222,128,0.55); }
@media (prefers-reduced-motion: reduce){ .field.invalid{ animation:none; } }

/* ============================= GLOBAL AMBIENT SPOTLIGHT ============================= */
.global-spotlight{
  position:fixed; inset:0; z-index:1; pointer-events:none;
  background:radial-gradient(600px circle at var(--sx,50%) var(--sy,20%), rgba(232,199,102,0.05), transparent 70%);
  transition:opacity .4s ease; opacity:0; mix-blend-mode:screen;
}
.global-spotlight.on{opacity:1;}

.lens-grid{
  position:fixed; inset:0; z-index:2; pointer-events:none; opacity:0;
  background-image:
    linear-gradient(rgba(232,199,102,0.5) 1px, transparent 1px),
    linear-gradient(90deg, rgba(143,160,232,0.5) 1px, transparent 1px);
  background-size:40px 40px;
  -webkit-mask-image:radial-gradient(220px circle at var(--sx,50%) var(--sy,20%), #000 0%, transparent 75%);
  mask-image:radial-gradient(220px circle at var(--sx,50%) var(--sy,20%), #000 0%, transparent 75%);
  transition:opacity .3s ease;
}
.lens-grid.on{opacity:.5;}

.lens-ring{
  position:fixed; z-index:3; pointer-events:none;
  width:220px; height:220px; border-radius:50%;
  border:1px solid rgba(232,199,102,0.28);
  left:var(--sx,50%); top:var(--sy,20%); transform:translate(-50%,-50%);
  opacity:0; transition:opacity .3s ease, width .3s ease, height .3s ease;
  box-shadow:inset 0 0 40px rgba(232,199,102,0.10);
}
.lens-ring.on{opacity:1;}
/* ============================= FILM GRAIN TEXTURE ============================= */
.grain-overlay{
  position:fixed; inset:0; z-index:2600; pointer-events:none;
  opacity:.035; mix-blend-mode:overlay;
  background-image:url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='120' height='120'><filter id='n'><feTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='2' stitchTiles='stitch'/></filter><rect width='100%25' height='100%25' filter='url(%23n)'/></svg>");
  background-size:120px 120px;
}
@media (prefers-reduced-motion: reduce){ .grain-overlay{opacity:.02;} }

/* ============================= CUSTOM MAGNETIC CURSOR ============================= */
.has-custom-cursor, .has-custom-cursor *{cursor:none !important;}
.cursor-dot, .cursor-ring{
  position:fixed; top:0; left:0; z-index:4000; pointer-events:none;
  border-radius:50%; transform:translate(-50%,-50%);
  opacity:0; transition:opacity .3s ease;
}
.has-custom-cursor .cursor-dot, .has-custom-cursor .cursor-ring{opacity:1;}
.cursor-dot{width:7px;height:7px;background:var(--amber-2); box-shadow:0 0 10px rgba(232,199,102,0.8);}
.cursor-ring{
  width:34px;height:34px; border:1.4px solid rgba(232,199,102,0.55);
  display:flex; align-items:center; justify-content:center;
  transition:opacity .3s ease, transform .18s ease, width .3s var(--ease), height .3s var(--ease),
             border-color .25s ease, border-radius .3s var(--ease), background .3s ease;
  font-family:var(--font-eyebrow); font-size:10.5px; font-weight:700; letter-spacing:.04em;
  text-transform:uppercase; color:var(--navy-ink); white-space:nowrap; overflow:hidden;
}
.cursor-ring.hover{
  width:54px;height:54px; border-color:var(--amber-2); background:rgba(232,199,102,0.08);
}
/* Buttons / links → pill with label */
.cursor-ring.mode-view{
  width:72px; height:72px; border-radius:50%;
  background:var(--navy-ink); border-color:var(--navy-ink); color:var(--ivory);
}
/* Cards → square-ish frame, like a "select" tool */
.cursor-ring.mode-card{
  width:64px; height:64px; border-radius:14px;
  background:rgba(27,42,107,0.06); border-color:var(--teal-deep);
}
/* Text inputs → thin vertical bar */
.cursor-ring.mode-text{
  width:2px; height:26px; border-radius:2px;
  background:var(--teal-deep); border-color:transparent;
}
/* Drag/chip elements → smaller dot-only */
.cursor-ring.mode-drag{
  width:16px; height:16px; border-radius:50%;
  background:var(--amber-2); border-color:transparent;
}
.cursor-ring span{ opacity:0; transform:scale(.6); transition:opacity .25s ease .1s, transform .25s ease .1s; }
.cursor-ring.mode-view span{ opacity:1; transform:scale(1); }
@media (prefers-reduced-motion: reduce){ .cursor-ring{ transition:opacity .2s ease; } }

/* ============================= PREMIUM GLOW CTA ============================= */
@property --cang{syntax:'<angle>'; initial-value:0deg; inherits:false;}
.btn-premium-glow{position:relative;}
.btn-premium-glow::before{
  content:''; position:absolute; inset:-2px; border-radius:100px; z-index:-1;
  background:conic-gradient(from var(--cang), var(--amber-2), transparent 20%, var(--amber) 45%, transparent 70%, var(--amber-2));
  animation:spinBorder 4s linear infinite;
  opacity:0; transition:opacity .35s ease;
}
.btn-premium-glow:hover::before{opacity:1;}
@media (prefers-reduced-motion: reduce){ .btn-premium-glow::before{animation:none;} }

/* ============================= EXTRA TILT SURFACES ============================= */
.value-item, .stat-block, .c-row{ transform-style:preserve-3d; will-change:transform; transition:transform .3s var(--ease); }

/* ============================= CORE SERVICES: 3D FLIP ============================= */
.core-card{ perspective:1200px; height:auto; }
.core-flip{
  position:relative; width:100%; min-height:230px;
  transform-style:preserve-3d; transition:transform .7s var(--ease);
}
.core-card.flipped .core-flip{ transform:rotateY(180deg); }
.core-face{
  backface-visibility:hidden; -webkit-backface-visibility:hidden;
}
.core-face.front{ position:relative; }
.core-face.back{
  position:absolute; inset:0; transform:rotateY(180deg);
  display:flex; flex-direction:column; justify-content:center; align-items:flex-start;
  background:linear-gradient(160deg,var(--teal-deep),var(--navy-ink));
  border-radius:var(--radius-md); padding:26px; color:var(--ivory);
}
.core-face.back h4{color:var(--amber-2); font-size:15px; margin-bottom:10px;}
.core-face.back p{color:rgba(250,247,242,0.75); font-size:13.5px;}
.core-flip-hint{
  position:absolute; top:14px; right:14px; font-size:11px; color:#8F8B80;
  font-family:var(--font-eyebrow); letter-spacing:.05em; text-transform:uppercase;
  display:flex; align-items:center; gap:5px; cursor:pointer; z-index:2;
}
.core-flip-hint::before{ content:'⟲'; font-size:13px; }

/* ============================= MAGNETIC NAV ============================= */
.nav-links a{ display:inline-block; transition:transform .25s var(--ease), color .3s ease; }
.brand-logo, .brand-mark{ transform-style:preserve-3d; transition:transform .35s var(--ease); }

/* ============================= HERO SCROLL DEPTH ============================= */
.hero{ transform-style:preserve-3d; }
.hero-grid{ transition:transform .1s linear, opacity .1s linear; will-change:transform, opacity; }

/* ============================= FAQ 3D SPOTLIGHT ============================= */
.faq-item{ transform-style:preserve-3d; }
.faq-item::after{
  content:''; position:absolute; inset:0; border-radius:14px; pointer-events:none; z-index:0;
  background:radial-gradient(220px circle at var(--qx,50%) var(--qy,0%), rgba(232,199,102,0.10), transparent 60%);
  opacity:0; transition:opacity .35s ease;
}
.faq-item:hover::after{ opacity:1; }
.faq-q, .faq-a{ position:relative; z-index:1; }

/* ============================= 3D SPINNING BADGE ============================= */
.est-badge{ perspective:400px; }
.est-badge b{
  display:inline-block; transform-style:preserve-3d;
  animation:coinSpin 5s linear infinite;
}
@keyframes coinSpin{
  0%,100%{ transform:rotateY(0deg); }
  50%{ transform:rotateY(360deg); }
}
@media (prefers-reduced-motion: reduce){ .est-badge b{ animation:none; } }

.hero-ghost-text{
  position:absolute; top:50%; left:50%; transform:translate(-50%,-50%);
  font-family:var(--font-display); font-weight:700;
  font-size:clamp(90px, 22vw, 340px);
  letter-spacing:-.03em;
  color:transparent;
  -webkit-text-stroke:1px rgba(255,255,255,0.06);
  white-space:nowrap; pointer-events:none; z-index:0; user-select:none;
  mix-blend-mode:overlay;
}
#hero3d{
  position:absolute; inset:0; z-index:0; pointer-events:none; opacity:0;
  transition:opacity 1.2s ease, filter .4s ease;
  filter:blur(0px) saturate(1);
}
#hero3d.ready{opacity:.55;}
@media (max-width:768px){ #hero3d{opacity:.3 !important;} }

.section-dots{
  position:fixed; right:26px; top:50%; transform:translateY(-50%); z-index:900;
  display:flex; flex-direction:column; gap:14px;
}
@media (max-width:1100px){ .section-dots{display:none;} }
.section-dots a{
  width:10px; height:10px; border-radius:50%;
  background:rgba(28,28,26,0.18); border:1px solid rgba(28,28,26,0.18);
  position:relative; transition:all .35s var(--ease); display:block;
}
.section-dots a::after{
  content:attr(data-label); position:absolute; right:22px; top:50%; transform:translateY(-50%);
  font-family:var(--font-eyebrow); font-size:11px; font-weight:600; letter-spacing:.05em; text-transform:uppercase;
  color:var(--navy-ink); background:var(--ivory); padding:6px 12px; border-radius:8px;
  white-space:nowrap; opacity:0; pointer-events:none; box-shadow:var(--shadow-sm);
  transition:opacity .25s ease, transform .25s ease;
}
.section-dots a:hover::after{opacity:1; transform:translateY(-50%) translateX(-4px);}
.section-dots a.active{background:var(--amber); border-color:var(--amber); transform:scale(1.3);}


.dust-particle{
  position:fixed; z-index:3500; pointer-events:none; border-radius:50%;
  background:radial-gradient(circle, var(--amber-2), transparent 70%);
  animation:dustFly .7s ease-out forwards;
}
@keyframes dustFly{
  to{ transform:translate(var(--dx), var(--dy)) scale(0); opacity:0; }
}

.coverflow-wrap{ perspective:1400px; text-align:center; }
.coverflow-stage{
  position:relative; height:340px; display:flex; align-items:center; justify-content:center;
}
@media (min-width:640px){ .coverflow-stage{height:300px;} }
.cf-card{
  position:absolute; width:min(560px,86vw);
  background:linear-gradient(160deg, rgba(255,255,255,0.6), rgba(255,255,255,0.2));
  border:1px solid rgba(255,255,255,0.5);
  border-radius:var(--radius-lg);
  backdrop-filter:blur(14px);
  box-shadow:var(--shadow-sm);
  padding:36px 30px;
  transition:transform .6s var(--ease), opacity .6s var(--ease), filter .6s var(--ease);
  will-change:transform, opacity;
}
.cf-card .test-quote-mark{font-family:var(--font-display); font-size:44px; color:var(--amber); line-height:1;}
.cf-card .test-text{font-family:var(--font-display); font-size:16.5px; font-weight:500; color:var(--navy-ink); line-height:1.5; margin-top:4px;}
.cf-card .test-author{margin-top:18px; display:flex; align-items:center; justify-content:center; gap:12px;}
.cf-card.center{ transform:translateX(0) translateZ(0) rotateY(0deg); opacity:1; z-index:3; filter:blur(0); }
.cf-card.left{ transform:translateX(-62%) translateZ(-160px) rotateY(35deg) scale(.82); opacity:.55; z-index:2; filter:blur(1px); }
.cf-card.right{ transform:translateX(62%) translateZ(-160px) rotateY(-35deg) scale(.82); opacity:.55; z-index:2; filter:blur(1px); }
.cf-card.hidden-l{ transform:translateX(-100%) translateZ(-260px) rotateY(45deg) scale(.7); opacity:0; z-index:1; }
.cf-card.hidden-r{ transform:translateX(100%) translateZ(-260px) rotateY(-45deg) scale(.7); opacity:0; z-index:1; }

.area-globe-wrap{ height:220px; display:flex; align-items:center; justify-content:center; margin-bottom:28px; }
#areaGlobe{ width:220px; height:220px; }
@media (max-width:640px){ .area-globe-wrap{height:170px;} #areaGlobe{width:170px;height:170px;} }

.stats-band{ perspective:1000px; }
.stats-grid{ transform-style:preserve-3d; }
.stat-block{ transition:transform .15s linear; will-change:transform; }

.process-orb-track{ position:relative; height:0; grid-column:1/-1; grid-row:1; }
@media (min-width:900px){
  .process-orb{
    position:absolute; top:-96px; width:20px; height:20px; left:0;
    transform-style:preserve-3d; animation:processSpin 3s linear infinite;
    opacity:0; transition:opacity .5s ease;
  }
  .process-orb.show{ opacity:1; }
  .process-orb .face{
    position:absolute; width:20px; height:20px;
    background:linear-gradient(145deg,var(--amber-2),var(--amber));
    border:1px solid rgba(255,255,255,0.4);
  }
  .process-orb .f1{ transform:translateZ(10px); }
  .process-orb .f2{ transform:rotateY(90deg) translateZ(10px); }
  .process-orb .f3{ transform:rotateY(180deg) translateZ(10px); }
  .process-orb .f4{ transform:rotateY(-90deg) translateZ(10px); }
  .process-orb .f5{ transform:rotateX(90deg) translateZ(10px); }
  .process-orb .f6{ transform:rotateX(-90deg) translateZ(10px); }
  @keyframes processSpin{ from{transform:rotateX(0) rotateY(0);} to{transform:rotateX(360deg) rotateY(360deg);} }
}

.faq-item.open{
  box-shadow:
    0 1px 0 rgba(28,28,26,0.04),
    0 8px 0 -4px rgba(28,28,26,0.03),
    0 16px 24px -12px rgba(10,17,40,0.15);
}

/* ============================= HOW IT WORKS v2 — 3D FLIP STEPPER ============================= */
.hiw-track{position:relative; display:grid; grid-template-columns:1fr; gap:26px; margin-top:20px; perspective:1600px;}
@media (min-width:900px){ .hiw-track{grid-template-columns:repeat(5,1fr); gap:20px;} }
.hiw-line{display:none;}
@media (min-width:900px){
  .hiw-line{display:block; position:absolute; top:54px; left:6%; right:6%; height:2px; z-index:0; background:var(--line); overflow:hidden; border-radius:2px;}
  .hiw-line-fill{position:absolute; inset:0; width:0%; border-radius:2px; background:linear-gradient(90deg,var(--amber),var(--amber-2),var(--teal-2)); box-shadow:0 0 14px rgba(232,199,102,0.6); transition:width 1.2s var(--ease);}
}
.hiw-card{position:relative; z-index:1; height:230px; cursor:pointer; perspective:1200px;}
.hiw-inner{position:relative; width:100%; height:100%; transform-style:preserve-3d; transition:transform .7s var(--ease);}
.hiw-card.flipped .hiw-inner{transform:rotateY(180deg);}
.hiw-face{position:absolute; inset:0; backface-visibility:hidden; -webkit-backface-visibility:hidden; border-radius:var(--radius-md); padding:26px 22px; display:flex; flex-direction:column;}
.hiw-front{background:var(--ivory); border:1px solid var(--line); box-shadow:var(--shadow-sm); transition:transform .4s var(--ease), box-shadow .4s var(--ease), border-color .3s ease;}
.hiw-card:not(.flipped):hover .hiw-front{transform:translateY(-6px); box-shadow:var(--shadow-lg); border-color:transparent;}
.hiw-num{font-family:var(--font-display); font-weight:700; font-size:13px; color:var(--amber); position:absolute; top:20px; right:22px;}
.hiw-icon{width:56px; height:56px; border-radius:16px; margin-bottom:18px; display:flex; align-items:center; justify-content:center; font-size:22px; background:linear-gradient(145deg,var(--teal-deep),var(--navy-ink)); color:var(--teal-2); box-shadow:0 14px 26px -14px rgba(10,17,40,0.5); transition:transform .45s var(--ease);}
.hiw-card:hover .hiw-icon{transform:rotate(-8deg) scale(1.08);}
.hiw-front h4{font-size:17px; margin-bottom:8px;}
.hiw-front p{font-size:13.5px;}
.hiw-flip-hint{margin-top:auto; font-size:11px; color:#8F8B80; font-family:var(--font-eyebrow); letter-spacing:.05em; text-transform:uppercase; display:flex; align-items:center; gap:5px;}
.hiw-flip-hint::before{content:'⟲'; font-size:13px;}
.hiw-back{transform:rotateY(180deg); background:linear-gradient(160deg,var(--teal-deep),var(--navy-ink)); color:var(--ivory); justify-content:center; box-shadow:var(--shadow-lg);}
.hiw-back h5{color:var(--amber-2); font-size:14px; margin-bottom:10px; font-family:var(--font-display);}
.hiw-back p{color:rgba(250,247,242,0.78); font-size:13px; line-height:1.6;}
@media (prefers-reduced-motion: reduce){ .hiw-inner{transition:opacity .3s ease;} .hiw-card.flipped .hiw-inner{transform:none;} }

/* ============================= INDUSTRIES v2 — 3D GLOW GRID ============================= */
.ind-grid{display:grid; grid-template-columns:repeat(2,1fr); gap:18px; perspective:1400px;}
@media (min-width:640px){ .ind-grid{grid-template-columns:repeat(3,1fr); gap:22px;} }
@media (min-width:1024px){ .ind-grid{grid-template-columns:repeat(6,1fr);} }
.ind-card{position:relative; overflow:hidden; border-radius:var(--radius-md); padding:28px 20px; min-height:200px; display:flex; flex-direction:column; justify-content:flex-end; color:var(--ivory); transform-style:preserve-3d; will-change:transform; transition:transform .45s var(--ease), box-shadow .45s var(--ease); border:1px solid rgba(255,255,255,0.08);}
.ind-card:nth-child(6n+1){background:linear-gradient(160deg,var(--teal-deep),var(--navy-ink));}
.ind-card:nth-child(6n+2){background:linear-gradient(160deg,var(--amber-deep),#5c4409);}
.ind-card:nth-child(6n+3){background:linear-gradient(160deg,var(--navy-2),var(--navy-ink));}
.ind-card:nth-child(6n+4){background:linear-gradient(160deg,#3a2f6b,var(--navy-ink));}
.ind-card:nth-child(6n+5){background:linear-gradient(160deg,var(--teal-deep),#0b1330);}
.ind-card:nth-child(6n+6){background:linear-gradient(160deg,var(--amber-deep),var(--navy-ink));}
.ind-card::before{content:''; position:absolute; inset:0; z-index:0; pointer-events:none; background:radial-gradient(220px circle at var(--mx,50%) var(--my,20%), rgba(255,255,255,0.16), transparent 60%); opacity:0; transition:opacity .4s ease;}
.ind-card:hover::before{opacity:1;}
.ind-card:hover{box-shadow:0 30px 60px -22px rgba(10,17,40,0.55);}
.ind-card > *{position:relative; z-index:1;}
.ind-icon{width:44px; height:44px; border-radius:12px; margin-bottom:14px; display:flex; align-items:center; justify-content:center; font-size:19px; background:rgba(255,255,255,0.12); border:1px solid rgba(255,255,255,0.18); transition:transform .4s var(--ease);}
.ind-card:hover .ind-icon{transform:translateZ(20px) rotate(-8deg) scale(1.1);}
.ind-card h4{color:var(--ivory); font-size:16.5px; margin-bottom:6px; transition:transform .4s var(--ease);}
.ind-card:hover h4{transform:translateZ(14px);}
.ind-desc{font-size:12.5px; color:rgba(250,247,242,0.65); line-height:1.5; max-height:0; opacity:0; overflow:hidden; transition:max-height .4s var(--ease), opacity .4s ease;}
.ind-card:hover .ind-desc{max-height:80px; opacity:1;}

/* ============================= MOBILE-FIRST FIXES ============================= */

/* Prevent any horizontal scroll from 3D-transformed elements */
html, body{ max-width:100%; overflow-x:hidden; }
.coverflow-stage{ overflow:hidden; }

@media (hover: none), (pointer: coarse){
  /* Kill hover-driven transforms that get stuck after a tap on touch devices */
  .core-card:hover, .core-card:active,
  .why-cell:hover, .why-cell:active,
  .industry-card:hover, .industry-card:active,
  .prod-card:hover,
  .ind-card:hover,
  .hiw-card:not(.flipped):hover .hiw-front,
  .test-card:hover,
  .cf-card:hover{
    transform:none !important;
  }

  /* Industries: always show the description text since :hover can't reveal it on touch */
  .ind-desc{
    max-height:80px; opacity:1; margin-top:2px;
  }
  .ind-card::before{ display:none; } /* glare layer needs a cursor position we'll never get */

  /* Turn off cursor-tracked glare/spotlight layers — they rely on mousemove, which never fires on touch */
  .core-glare, .prod-glare, .prod-shine, .test-card::before,
  .contact-panel::before, footer::before, .products-band-spotlight,
  .global-spotlight, .lens-grid, .lens-ring, .cursor-dot, .cursor-ring{
    display:none !important;
  }
}

/* How It Works: let the card grow to fit wrapped text instead of clipping at 230px */
@media (max-width:640px){
  .hiw-card{ height:auto; }
  .hiw-inner{ height:auto; }
  .hiw-face.hiw-front{ position:relative; min-height:230px; }
  .hiw-face.hiw-back{ min-height:230px; }
}

/* Tighter spacing + type scale on very small phones */
@media (max-width:380px){
  .pad{ padding:48px 0; }
  .section-head h2{ font-size:22px; }
  .hero-content h1{ font-size:30px; }
  .prod-card, .core-card{ padding-left:18px; padding-right:18px; }
  .ind-card{ padding:22px 16px; min-height:170px; }
  .hiw-face{ padding:22px 18px; }
}

/* ============================= FAQ: FIX DISAPPEARING ANSWER ON MOBILE ============================= */
@media (hover: none), (pointer: coarse), (max-width:900px){
  .faq-a-panel{
    /* Ditch the 3D flip on mobile — rotateX without backface-visibility
       causes the panel to render its backface (invisible) mid-transition
       on many mobile browsers, making the answer flash and disappear. */
    transform:none !important;
    transform-origin:initial;
    max-height:0;
    opacity:0;
    transition:max-height .4s var(--ease), opacity .35s ease;
  }
  .faq-item.open .faq-a-panel{
    transform:none !important;
    max-height:600px;
    opacity:1;
    transition:max-height .45s var(--ease), opacity .4s ease .05s;
  }
  .faq-a-panel p{
    opacity:1 !important;
    transform:none !important;
    transition:none !important;
  }
}
</style>

<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "ProfessionalService",
  "name": "Samal Consultancy",
  "image": "https://www.samalconsultancy.com/assets/images/og-cover.jpg",
  "url": "https://www.samalconsultancy.com/",
  "telephone": "+91-7002050242",
  "priceRange": "₹₹",
  "founder": {"@type": "Person", "name": "Subham Kumar Samal"},
  "foundingDate": "1993",
  "address": {
    "@type": "PostalAddress",
    "streetAddress": "Subarna Path, Near Railway Gate, Lakhinagar, Khutikatia",
    "addressLocality": "Nagaon",
    "addressRegion": "Assam",
    "postalCode": "782002",
    "addressCountry": "IN"
  },
  "geo": {"@type": "GeoCoordinates", "latitude": "26.3480", "longitude": "92.6840"},
  "areaServed": [
    {"@type":"City","name":"Nagaon"},
    {"@type":"City","name":"Tezpur"},
    {"@type":"City","name":"Guwahati"},
    {"@type":"City","name":"Jorhat"},
    {"@type":"City","name":"Dibrugarh"},
    {"@type":"City","name":"Silchar"},
    {"@type":"State","name":"Assam"}
  ],
  "openingHoursSpecification": {
    "@type": "OpeningHoursSpecification",
    "dayOfWeek": ["Monday","Tuesday","Wednesday","Thursday","Friday","Saturday"],
    "opens": "10:00",
    "closes": "19:00"
  },
  "makesOffer": [
    {"@type":"Offer","itemOffered":{"@type":"Service","name":"GST Registration & Return Filing"}},
    {"@type":"Offer","itemOffered":{"@type":"Service","name":"EPFO & ESIC Compliance"}},
    {"@type":"Offer","itemOffered":{"@type":"Service","name":"Company & LLP Registration"}},
    {"@type":"Offer","itemOffered":{"@type":"Service","name":"Trademark Registration"}},
    {"@type":"Offer","itemOffered":{"@type":"Service","name":"Payroll & Bookkeeping"}}
  ]
}
</script>

<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": [
    {"@type":"Question","name":"What are your service charges?","acceptedAnswer":{"@type":"Answer","text":"Charges vary by service and business complexity — every quote is itemised upfront, with no hidden add-ons."}},
    {"@type":"Question","name":"How long does company registration take?","acceptedAnswer":{"@type":"Answer","text":"Once documentation is complete, most private limited or LLP registrations are finalised within 10–15 working days."}},
    {"@type":"Question","name":"What documents are needed for GST registration?","acceptedAnswer":{"@type":"Answer","text":"PAN, Aadhaar of directors or partners, address proof of the place of business, a bank statement, digital signature and a letter of authorisation."}},
    {"@type":"Question","name":"Do you handle trademark objections?","acceptedAnswer":{"@type":"Answer","text":"Yes, from search and filing through to responding to examination objections and securing registration."}},
    {"@type":"Question","name":"Can you manage payroll for a distributed team?","acceptedAnswer":{"@type":"Answer","text":"Yes, payroll processing, statutory deductions and compliance filings are covered regardless of location."}},
    {"@type":"Question","name":"Do you offer ongoing compliance monitoring, or only one-off filings?","acceptedAnswer":{"@type":"Answer","text":"Both — most clients are on an ongoing retainer with a compliance calendar and renewal alerts."}}
  ]
}
</script>
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": "SoftwareApplication",
      "name": "Taskvel Free",
      "url": "https://www.samalconsultancy.com/taskvel-free.php",
      "applicationCategory": "ProductivityApplication",
      "operatingSystem": "Any (Web Browser)",
      "description": "Free browser-based task tracker and to-do organizer with smart quick-add, Pomodoro focus timer, priority matrix and offline PWA support. No account or sign-up required.",
      "offers": { "@type": "Offer", "price": "0", "priceCurrency": "USD" },
      "publisher": { "@type": "Organization", "name": "Samal Consultancy" }
    },
    {
      "@type": "SoftwareApplication",
      "name": "Taskvel Premium",
      "url": "https://www.samalconsultancy.com/taskvel-pro.php",
      "applicationCategory": "BusinessApplication",
      "operatingSystem": "Web",
      "description": "Premium task management and productivity OS with team collaboration, multi-device sync, analytics dashboard, and a compliance & client management module.",
      "offers": { "@type": "Offer", "category": "Subscription" },
      "publisher": { "@type": "Organization", "name": "Samal Consultancy" }
    }
  ]
}
</script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/lenis/1.1.13/lenis.min.js"></script>
</head>
<body>
<div class="global-spotlight" id="globalSpotlight" aria-hidden="true"></div>
<div class="lens-grid" id="lensGrid" aria-hidden="true"></div>
<div class="lens-ring" id="lensRing" aria-hidden="true"></div>
<div class="grain-overlay" aria-hidden="true"></div>
<div class="cursor-ring" id="cursorRing" aria-hidden="true"><span id="cursorRingLabel"></span></div>
<div class="cursor-dot" id="cursorDot" aria-hidden="true"></div>


<!-- ============================= PAGE LOAD SEQUENCE ============================= -->
<div class="preloader" id="preloader" aria-hidden="true">
  <div class="preloader-mark">
    <svg viewBox="0 0 44 44" width="44" height="44" fill="none">
      <circle class="pl-ring" cx="22" cy="22" r="18" stroke="rgba(232,199,102,0.18)" stroke-width="2.5"/>
      <circle class="pl-arc" cx="22" cy="22" r="18" stroke="url(#plGrad)" stroke-width="2.5" stroke-linecap="round"/>
      <defs><linearGradient id="plGrad" x1="0" y1="0" x2="44" y2="44"><stop offset="0" stop-color="#E8C766"/><stop offset="1" stop-color="#8FA0E8"/></linearGradient></defs>
    </svg>
  </div>
  <span class="preloader-label">Samal Consultancy</span>
</div>

<div class="scroll-progress" id="scrollProgress" aria-hidden="true"></div>
<div class="route-transition-bar" id="routeBar" aria-hidden="true"></div>

<!-- ============================= NAVBAR ============================= -->
<nav class="navbar" id="navbar">
  <div class="wrap nav-inner">
    <a href="#top" class="brand">
        <img src="images/3.png"
          alt="Samal Consultancy Logo"
          class="brand-logo"
          id="navLogoImg"
          onerror="
              this.style.display='none';
              document.getElementById('navLogoFallback').style.display='flex';
          ">

        <span class="brand-mark" id="navLogoFallback" style="display:none;">SC</span>

        <span class="brand-name">Samal Consultancy</span>
    </a>
    
    <ul class="nav-links" id="navLinks">
      <li><a href="#about">About</a></li>
      <li><a href="#services">Services</a></li>
      <li><a href="#registry">Registry</a></li>
      <li><a href="#process">Process</a></li>
      <li><a href="#areas">Areas</a></li>
      <li><a href="#clients">Clients</a></li>
      <li><a href="#contact">Contact</a></li>
      <li><a href="#products">Products</a></li>
    </ul>
    <div class="nav-cta">
      <a href="#contact" class="btn btn-gold btn-premium-glow" data-ripple style="padding:12px 20px; font-size:13px;">Book Consultation<span class="btn-shine" aria-hidden="true"></span></a>
      <button class="menu-toggle" id="menuToggle" aria-label="Toggle menu"><span></span><span></span><span></span></button>
    </div>
  </div>
</nav>

<!-- ============================= HERO ============================= -->
<header class="hero" id="top">
  <div class="hero-orbs" aria-hidden="true">
    <span class="orb orb-1" data-depth="0.06"></span>
    <span class="orb orb-2" data-depth="0.1"></span>
    <span class="orb orb-3" data-depth="0.14"></span>
  </div>
  <div class="hero-ghost-text" data-depth="0.03" aria-hidden="true">TRUST</div>
  <canvas id="hero3d" aria-hidden="true"></canvas>
  <div class="hero-particles" id="heroParticles" aria-hidden="true"></div>
  <div class="cursor-glow" id="cursorGlow" aria-hidden="true"></div>
  <div class="wrap hero-grid">
    <div class="hero-content" id="heroContent">
      <div class="est-badge load-in" style="--d:0">Est. <b>1993</b> &nbsp;·&nbsp; 30+ Years in Practice</div>
      <span class="eyebrow load-in" style="--d:1">Business Compliance &amp; Advisory</span>
      <h1 class="load-in" style="--d:2">GST, Company &amp; Compliance Consultancy in <em>Nagaon, Tezpur &amp; Assam</em>. Every deadline, met.</h1>
      <p class="hero-tagline load-in" style="--d:3">Providing trusted compliance services since 1993.</p>
      <p class="lead load-in" style="--d:4">Samal Consultancy runs the tax, registration and labour-law compliance behind 70+ businesses across Assam — so nothing slips, nothing lapses, and nothing is left to chance.</p>
      <div class="hero-actions load-in" style="--d:5">
        <a href="#contact" class="btn btn-gold btn-premium-glow" data-ripple>Get Free Consultation<span class="btn-shine" aria-hidden="true"></span></a>
        <a href="#services" class="btn btn-ghost" data-ripple>Explore Services<span class="btn-shine" aria-hidden="true"></span></a>
      </div>
      <div class="trust-row load-in" style="--d:6">
        <div><div class="t-num">70+</div><div class="t-label">Businesses served</div></div>
        <div><div class="t-num">99.2%</div><div class="t-label">On-time filing rate</div></div>
        <div><div class="t-num">30+ yrs</div><div class="t-label">In practice</div></div>
      </div>
    </div>

    <div class="hero-visual load-in" style="--d:3;" data-plane="mid">
      <div class="ledger-card">
        <div class="ledger-top">
          <div class="ledger-title"><span class="ledger-dot"></span> Live Compliance Ledger</div>
          <div class="ledger-clock" id="ledgerClock">--:--:--</div>
        </div>
        <div class="ledger-rows" id="ledgerRows">
          <div class="ledger-skeleton"></div>
          <div class="ledger-skeleton"></div>
          <div class="ledger-skeleton"></div>
        </div>
        <div class="ledger-foot">
          <div><div class="score">100%</div><div class="score-label">Filed before deadline, this cycle</div></div>
          <div style="text-align:right;"><div class="score" style="font-size:13px;color:var(--ivory);">GST · EPFO · ESIC</div><div class="score-label">Auto-reconciled</div></div>
        </div>
      </div>
    </div>
  </div>
</header>

<!-- ============================= MARQUEE ============================= -->
<div class="marquee-band">
  <div class="marquee-track" id="marqueeTrack"></div>
</div>

<!-- ============================= ABOUT ============================= -->
<section class="pad" id="about">
  <div class="wrap about-grid">
    <div class="reveal">
      <div class="about-visual" data-plane="near">
        <span class="quote-mark">"</span>
        <p>We don't sell paperwork. We sell the certainty that your business is standing on solid legal ground.</p>
        <div class="sig">— Subham Kumar Samal, Managing Partner</div>
      </div>
      <div class="trust-orbit-wrap" aria-hidden="true">
        <div class="trust-orbit" id="trustOrbit"></div>
      </div>
    </div>
    <div>
      <div class="reveal">
        <span class="eyebrow">Who we are</span>
        <h2 style="font-size:clamp(24px,5vw,38px); margin-top:14px; line-height:1.22;">Three decades of keeping ambitious businesses on the right side of every regulation.</h2>
        <p style="margin-top:16px; font-size:15.5px; max-width:560px;">Founded in 1993, Samal Consultancy began as a small tax advisory desk and has grown into a full-stack compliance practice — spanning taxation, registrations, labour law, accounting and corporate governance — trusted by startups, MSMEs and exporters across India.</p>
      </div>
      <div class="value-list reveal">
        <div class="value-item">
          <span class="value-num">01</span>
          <div><h4>Vision</h4><p>To be the most trusted compliance partner for growing businesses, nationwide.</p></div>
        </div>
        <div class="value-item">
          <span class="value-num">02</span>
          <div><h4>Mission</h4><p>To simplify compliance and remove regulatory friction from business growth.</p></div>
        </div>
        <div class="value-item">
          <span class="value-num">03</span>
          <div><h4>Values</h4><p>Integrity, accuracy, and a genuine sense of urgency around your deadlines.</p></div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ============================= STATS ============================= -->
<section class="stats-band">
  <div class="wrap stats-grid">
    <div class="stat-block reveal"><div class="stat-num" data-count="70" data-suffix="+">0</div><div class="stat-label">Clients served</div></div>
<div class="stat-block reveal" style="transition-delay:.08s;"><div class="stat-num" data-count="40" data-suffix="+">0</div><div class="stat-label">Active clients</div></div>
<div class="stat-block reveal" style="transition-delay:.16s;"><div class="stat-num" data-count="40" data-suffix="+">0</div><div class="stat-label">EPFO &amp; ESIC registrations</div></div>
<div class="stat-block reveal" style="transition-delay:.24s;"><div class="stat-num" data-count="30" data-suffix="+">0</div><div class="stat-label">Years of experience</div></div>
  </div>
</section>

<!-- ============================= CORE SERVICES ============================= -->
<section class="pad services-bg" id="services">
  <div class="wrap">
    <div class="section-head reveal">
      <span class="eyebrow">Core Services</span>
      <h2>GST, EPFO/ESIC and company registration — the four pillars businesses across Assam lean on us for most.</h2>
    </div>
    <div class="core-grid flip-in-grid reveal">
      <div class="core-card" style="--i:0" data-glare><span class="core-glare" aria-hidden="true"></span>
        <div class="core-flip">
          <span class="core-flip-hint">Flip</span>
          <div class="core-face front"><div class="core-icon">◈</div><h3>GST Registration &amp; Return Filing</h3><p>End-to-end GST — registration, monthly/annual returns, reconciliation and notice handling.</p><a href="#registry" class="core-link">View scope <span class="arrow">→</span></a></div>
          <div class="core-face back"><h4>Why it matters</h4><p>Late or incorrect GST filings trigger interest, penalties and blocked e-way bills — we keep every return filed before the deadline, every cycle.</p></div>
        </div>
      </div>
      <div class="core-card" style="--i:1" data-glare><span class="core-glare" aria-hidden="true"></span>
        <div class="core-flip">
          <span class="core-flip-hint">Flip</span>
          <div class="core-face front"><div class="core-icon">◆</div><h3>EPFO &amp; ESIC Compliance</h3><p>Registration, monthly ECR filing, UAN activation and employee benefit administration.</p><a href="#registry" class="core-link">View scope <span class="arrow">→</span></a></div>
          <div class="core-face back"><h4>Why it matters</h4><p>Missed ECR filings put employee PF and insurance benefits at risk — our team tracks every due date so your workforce stays covered.</p></div>
        </div>
      </div>
      <div class="core-card" style="--i:2" data-glare><span class="core-glare" aria-hidden="true"></span>
        <div class="core-flip">
          <span class="core-flip-hint">Flip</span>
          <div class="core-face front"><div class="core-icon">◇</div><h3>Business Registrations</h3><p>Udyam, FSSAI, IEC, Trademark, and Shop &amp; Establishment — set up correctly, first time.</p><a href="#registry" class="core-link">View scope <span class="arrow">→</span></a></div>
          <div class="core-face back"><h4>Why it matters</h4><p>A wrong registration category can cost months of rework — we map the right licenses to your business from day one.</p></div>
        </div>
      </div>
      <div class="core-card" style="--i:3" data-glare><span class="core-glare" aria-hidden="true"></span>
        <div class="core-flip">
          <span class="core-flip-hint">Flip</span>
          <div class="core-face front"><div class="core-icon">◉</div><h3>Payroll &amp; Bookkeeping</h3><p>Accurate payroll, statutory deductions and books that are always audit-ready.</p><a href="#registry" class="core-link">View scope <span class="arrow">→</span></a></div>
          <div class="core-face back"><h4>Why it matters</h4><p>Clean books mean faster loans, smoother audits and no year-end scramble — we reconcile every month, not just at filing time.</p></div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ============================= DETAILED SERVICES / REGISTRY ============================= -->
<section class="pad" id="registry">
  <div class="wrap">
    <div class="section-head reveal">
      <span class="eyebrow">The Full Registry</span>
      <h2>Every service, organised by category. Tap a category to see the full list.</h2>
    </div>
    <div class="acc-list reveal" id="accList"></div>
  </div>
</section>

<!-- ============================= WHY US ============================= -->
<section class="pad" style="background:var(--paper);" id="why">
  <div class="wrap">
    <div class="section-head reveal">
      <span class="eyebrow">Why Samal Consultancy</span>
      <h2>What changes when we take over your compliance.</h2>
    </div>
    <div class="why-grid reveal">
      <div class="why-cell"><span class="why-icon">◈</span><h4>Expert Professionals</h4><p>CAs, CS and labour-law specialists on every file.</p></div>
      <div class="why-cell"><span class="why-icon">◆</span><h4>Fast Turnaround</h4><p>Structured workflows that beat statutory deadlines.</p></div>
      <div class="why-cell"><span class="why-icon">◇</span><h4>Transparent Pricing</h4><p>Quoted upfront. No surprise charges, ever.</p></div>
      <div class="why-cell"><span class="why-icon">◉</span><h4>Dedicated Support</h4><p>One point of contact who knows your file end to end.</p></div>
      <div class="why-cell"><span class="why-icon">◈</span><h4>Accurate Compliance</h4><p>Double-reviewed filings, reconciled every cycle.</p></div>
      <div class="why-cell"><span class="why-icon">◆</span><h4>Secure Documentation</h4><p>Encrypted storage and controlled document access.</p></div>
      <div class="why-cell"><span class="why-icon">◇</span><h4>Digital-First Process</h4><p>Upload, e-sign and track — no paper trail required.</p></div>
      <div class="why-cell"><span class="why-icon">◉</span><h4>Trusted Since 1993</h4><p>Three decades of practice, with generations of repeat clients.</p></div>
    </div>
  </div>
</section>

<!-- ============================= PROCESS ============================= -->
<!-- ============================= PROCESS ============================= -->
<section class="pad process-strip" id="process">
  <div class="wrap">
    <div class="section-head reveal">
      <span class="eyebrow">How It Works</span>
      <h2>Five steps from onboarding to ongoing peace of mind. Tap a card to flip it.</h2>
    </div>
    <div class="hiw-track reveal" id="hiwTrack">
      <div class="hiw-line" aria-hidden="true"><div class="hiw-line-fill" id="hiwLineFill"></div></div>
    </div>
  </div>
</section>

<!-- ============================= INDUSTRIES ============================= -->
<section class="pad" style="background:var(--paper);">
  <div class="wrap">
    <div class="section-head reveal">
      <span class="eyebrow">Industries We Serve</span>
      <h2>Compliance experience across the sectors that build Assam's economy.</h2>
    </div>
    <div class="ind-grid reveal" id="indGrid"></div>
  </div>
</section>

<!-- ============================= SERVICE AREAS ============================= -->
<section class="pad" id="areas">
  <div class="wrap">
    <div class="section-head reveal">
      <span class="eyebrow">Where We Work</span>
      <h2>Serving businesses across Nagaon, Tezpur, Guwahati and Assam.</h2>
      <p>Based in Nagaon, Samal Consultancy handles GST, company registration and compliance filings for clients across Assam — including Tezpur, Guwahati, Jorhat, Dibrugarh and Silchar — all managed digitally.</p>
    </div>
    <div class="area-globe-wrap reveal" data-plane="far">
      <canvas id="areaGlobe" aria-hidden="true"></canvas>
    </div>
    <div class="industry-scroll reveal">
      <div class="industry-card"><div class="n">01</div><h4>Nagaon</h4></div>
      <div class="industry-card"><div class="n">02</div><h4>Tezpur</h4></div>
      <div class="industry-card"><div class="n">03</div><h4>Guwahati</h4></div>
      <div class="industry-card"><div class="n">04</div><h4>Jorhat</h4></div>
      <div class="industry-card"><div class="n">05</div><h4>Dibrugarh</h4></div>
      <div class="industry-card"><div class="n">06</div><h4>Silchar</h4></div>
    </div>
  </div>
</section>

<!-- ============================= TESTIMONIALS ============================= -->
<section class="pad" id="clients">
  <div class="wrap">
    <div class="coverflow-wrap reveal">
      <div class="coverflow-stage" id="coverflowStage"></div>
      <div class="test-dots" id="testDots"></div>
    </div>
  </div>
</section>

<!-- ============================= FAQ ============================= -->
<section class="pad" style="background:var(--paper);">
  <div class="wrap">
    <div class="section-head center reveal" style="margin-left:auto;margin-right:auto;">
      <span class="eyebrow">FAQ</span>
      <h2>Answers to what clients ask us first.</h2>
    </div>
    <div class="faq-wrap reveal">
      <div class="faq-search">
        <span class="sicon">⌕</span>
        <input type="text" id="faqSearch" placeholder="Search a question…" />
      </div>
      <div id="faqList"></div>
    </div>
  </div>
</section>

<!-- ============================= CONTACT ============================= -->
<section class="pad" id="contact">
  <div class="wrap">
    <div class="contact-panel reveal" data-glare-panel>
      <div>
        <span class="eyebrow" style="color:var(--teal-2);">Get In Touch</span>
        <h2 style="margin-top:14px;">Tell us what's due, and we'll take it from here.</h2>
        <p style="color:rgba(250,247,242,0.65); margin-top:12px;">Typical response time: under 4 business hours.</p>
        <div class="contact-list">

  <div class="c-row">
    <div class="c-icon">☎</div>
    <div>
      <h5>Phone</h5>
      <p>
        <a href="tel:+917002050242">+91 70020 50242</a> /
        <a href="tel:+918134044176">+91 81340 44176</a>
      </p>
    </div>
  </div>

  <div class="c-row">
    <div class="c-icon">✉</div>
    <div>
      <h5>Email</h5>
      <p>
        <a href="mailto:info@samalconsultancy.com">
          info@samalconsultancy.com
        </a>
      </p>
    </div>
  </div>

  <div class="c-row">
    <div class="c-icon">⌂</div>
    <div>
      <h5>Office</h5>
      <p>
        <a href="https://maps.google.com/?q=Subarna+Path,+Near+Railway+Gate,+Lakhinagar,+Khutikatia,+Nagaon,+Assam+782002"
           target="_blank"
           rel="noopener noreferrer">
          Subarna Path, Near Railway Gate, Lakhinagar, Khutikatia, Nagaon, Assam 782002
        </a>
      </p>
    </div>
  </div>

  <div class="c-row">
    <div class="c-icon">◷</div>
    <div>
      <h5>Business Hours</h5>
      <p>Monday – Saturday, 10:00 AM – 7:00 PM</p>
    </div>
  </div>

</div>
      </div>
      <form class="form-grid" id="contactForm" novalidate>
      <div class="two-col">
        <div class="field"><input type="text" name="name" placeholder=" " required id="fname"><label>Full name</label><span class="field-error" id="fname-err"></span></div>
        <div class="field"><input type="tel" name="phone" placeholder=" " required id="fphone" pattern="[0-9+\-\s]{7,15}"><label>Phone number</label><span class="field-error" id="fphone-err"></span></div>
      </div>
      <div class="field"><input type="email" name="email" placeholder=" " required id="femail"><label>Email address</label><span class="field-error" id="femail-err"></span></div>
      <div class="field"><textarea name="message" placeholder=" " required id="fmsg"></textarea><label>What do you need help with?</label><span class="field-error" id="fmsg-err"></span></div>

      <!-- honeypot: hidden from real visitors, bots tend to fill every field -->
      <div style="position:absolute;left:-9999px;top:auto;width:1px;height:1px;overflow:hidden;" aria-hidden="true">
        <input type="text" name="website" id="fwebsite" tabindex="-1" autocomplete="off">
      </div>

      <div id="formStatus" class="form-status" role="status" aria-live="polite" style="display:none;"></div>

      <button type="submit" class="btn btn-gold" data-ripple id="contactSubmitBtn">Send Message<span class="form-btn-shine" aria-hidden="true"></span></button>
    </form>
    </div>
  </div>
</section>

<!-- ============================= PRODUCTS & SOLUTIONS ============================= -->
<section class="pad products-band" id="products">
  <span class="orb p-orb-1" aria-hidden="true"></span>
  <span class="orb p-orb-2" aria-hidden="true"></span>
  <div class="products-band-spotlight" id="productsSpotlight" aria-hidden="true"></div>
  <div class="prod-particles" id="prodParticles" aria-hidden="true"></div>
  <div class="wrap">
    <div class="section-head center reveal">
      <span class="eyebrow">Built In-House</span>
      <h2>Our Products &amp; Solutions by Samal Consultancy</h2>
      <p>Beyond compliance — we build software that helps teams work smarter. Explore the tools we've crafted for productivity, collaboration and what's coming next.</p>
    </div>

    <div class="prod-grid stagger reveal">

      <!-- 1. TASKVEL FREE -->
      <article class="prod-card" style="--i:0" data-tilt>
        <span class="prod-glare" aria-hidden="true"></span>
        <span class="prod-shine" aria-hidden="true"></span>
        <div class="prod-top">
          <div class="prod-icon">✦</div>
          <span class="prod-badge free">Free · No Login</span>
        </div>
        <h3>Taskvel <em>Free</em></h3>
        <div class="prod-sub">Browser-based task manager</div>
        <p class="prod-desc">Open it and start working — no account, no sign-up, no friction. A fast, focused task organizer that runs entirely in your browser and keeps you in flow all day.</p>
        <ul class="prod-feats">
          <li>Smart Quick-Add with natural-language parsing — type "Call the client tomorrow #urgent" and it's tagged, dated and prioritised</li>
          <li>Pomodoro focus timer with a floating, draggable mini-timer and daily focus history</li>
          <li>Eisenhower Priority Matrix, smart tabs and a ⌘K command palette</li>
          <li>Productivity Score, fire-streak tracker and daily goal progress</li>
          <li>Offline-first PWA — installs like a native app, works without internet</li>
          <li>CSV, PDF, calendar (.ics) and full JSON backup exports</li>
        </ul>
        <div class="prod-actions">
          <a href="./taskvel-free.php" target="_blank" rel="noopener noreferrer" class="btn btn-gold" data-ripple>
              Try Now
              <span class="prod-btn-shine" aria-hidden="true"></span>
          </a>
          </div>
      </article>

      <!-- 2. TASKVEL PREMIUM -->
      <article class="prod-card premium" style="--i:1" data-tilt>
        <span class="prod-glare" aria-hidden="true"></span>
        <span class="prod-shine" aria-hidden="true"></span>
        <div class="prod-top">
          <div class="prod-icon">⚡</div>
          <span class="prod-badge gold">★ Premium</span>
        </div>
        <h3>Taskvel <em>Premium</em></h3>
        <div class="prod-sub">Productivity &amp; compliance OS · Laravel</div>
        <p class="prod-desc">The complete platform for individuals, teams and firms — secure accounts, real-time sync across every device, and a compliance engine built for professional practices.</p>
        <ul class="prod-feats">
          <li>Secure user authentication with role &amp; permission management</li>
          <li>Multi-device synchronization — your workspace everywhere</li>
          <li>Premium analytics dashboard with focus reports &amp; productivity trends</li>
          <li>Team collaboration, calendar planning and smart notifications</li>
          <li>Pomodoro focus system with custom sessions and session-level stats</li>
          <li>Compliance &amp; client management module — GST, EPF, ESIC trackers, payroll, invoicing and reminders</li>
        </ul>
        <div class="prod-actions">
          <a href="./taskvel-premium.php" target="_blank" rel="noopener noreferrer" class="btn btn-gold" data-ripple>
              Get Started
              <span class="prod-btn-shine" aria-hidden="true"></span>
          </a>
        </div>
      </article>

      <!-- 3. EVENTS NEAR YOU -->
      <article class="prod-card soon" style="--i:2" data-tilt>
        <span class="prod-glare" aria-hidden="true"></span>
        <span class="prod-shine" aria-hidden="true"></span>
        <div class="prod-top">
          <div class="prod-icon">◈</div>
          <span class="prod-badge soon-badge">Coming Soon</span>
        </div>
        <h3>Events <em>Near You</em></h3>
        <div class="prod-sub">Discover · Book · Experience</div>
        <p class="prod-desc">One platform for everything around an event — discover what's happening nearby, book tickets, find hotels and Airbnb stays, and plan the whole experience in a few taps.</p>
        <ul class="prod-feats">
          <li>Discover nearby events with galleries, schedules &amp; organizer details</li>
          <li>Book tickets, hotels and Airbnb stays in one place</li>
          <li>Restaurants, cafés and nearby attractions around every venue</li>
          <li>Google Maps directions, favourites, sharing &amp; organizer contact</li>
        </ul>
        <div class="prod-chips">
          <span class="prod-chip">Ticket Booking</span>
          <span class="prod-chip">Hotels</span>
          <span class="prod-chip">Airbnb</span>
          <span class="prod-chip">Restaurants</span>
          <span class="prod-chip">Maps</span>
          <span class="prod-chip">Galleries</span>
          <span class="prod-chip">Favourites</span>
          <span class="prod-chip">Schedules</span>
        </div>
        <div class="prod-actions">
          <a href="./events.php" target="_blank" rel="noopener noreferrer" class="btn btn-gold" data-ripple>
            View Events
            <span class="prod-btn-shine" aria-hidden="true"></span>
          </a>
        </div>
      </article>

    </div>

    <p class="prod-foot-note reveal">Built and maintained in-house · <a href="#contact">Talk to us</a> about custom software for your business.</p>
  </div>
</section>

<!-- ============================= FOOTER ============================= -->
<footer id="footer">
  <span class="footer-orb footer-orb-1" aria-hidden="true"></span>
  <span class="footer-orb footer-orb-2" aria-hidden="true"></span>

  <div class="footer-cta wrap reveal">
    <div class="footer-cta-text">
      <span class="eyebrow" style="color:var(--amber-2);">Let's Talk Compliance</span>
      <h3>Ready to hand off your GST, EPFO &amp; ROC filings?</h3>
    </div>
    <a href="#contact" class="btn btn-gold btn-premium-glow" data-ripple>Book Free Consultation<span class="btn-shine" aria-hidden="true"></span></a>
  </div>

  <div class="wrap">
    <div class="footer-grid">
      <div class="footer-brand">
        <a href="#top" class="brand" style="margin-bottom:6px;">
            <img src="images/3.png"
                alt="Samal Consultancy Logo"
                class="footer-logo"
                id="footerLogoImg"
                onerror="
                    this.style.display='none';
                    document.getElementById('footerLogoFallback').style.display='flex';
                ">

            <span class="brand-mark"
                id="footerLogoFallback"
                style="display:none; align-items:center; justify-content:center;">
                SC
            </span>
        </a>
        <span class="brand-name" style="color:var(--ivory);">Samal Consultancy</span>
        <p>End-to-end tax, registration and labour-law compliance for growing businesses.</p>
        <div class="footer-est">Providing trusted compliance services since 1993</div>
        <div class="footer-mini-contact">
          <a href="tel:+917002050242"><span class="fmc-icon">☎</span> +91 70020 50242</a>
          <a href="mailto:info@samalconsultancy.com"><span class="fmc-icon">✉</span> info@samalconsultancy.com</a>
        </div>
        <div class="social-row">
          <a href="#" aria-label="LinkedIn"><span>in</span></a>
          <a href="#" aria-label="Twitter"><span>𝕏</span></a>
          <a href="#" aria-label="Instagram"><span>◎</span></a>
        </div>
      </div>
      <div class="footer-col">
        <h5>Company</h5>
        <ul><li><a href="#about">About Us</a></li><li><a href="#products">Products</a></li><li><a href="#services">Services</a></li><li><a href="#process">Process</a></li><li><a href="#contact">Contact</a></li></ul>
      </div>
      <div class="footer-col">
        <h5>Services</h5>
        <ul><li><a href="#registry">GST Filing</a></li><li><a href="#registry">EPFO &amp; ESIC</a></li><li><a href="#registry">Registrations</a></li><li><a href="#registry">ROC Compliance</a></li></ul>
      </div>
      <div class="footer-col">
        <h5>Stay Updated</h5>
        <p style="font-size:14px; margin-bottom:4px; color: #e0bd57;">Compliance deadlines, in your inbox monthly.</p>
        <form class="sub-form" id="newsletterForm">
          <input type="email" name="email" id="newsletterEmail" placeholder="Your email" required>
          <button type="submit" id="newsletterBtn">Join</button>
        </form>
        <div id="newsletterStatus" style="font-size:12.5px; margin-top:8px; display:none;"></div>
      </div>
    </div>
    <div class="footer-bottom">
      <span>© <span id="year"></span> Samal Consultancy. All rights reserved.</span>
      <span>Privacy Policy · Terms of Service</span>
    </div>
  </div>
</footer>

<!-- ============================= BACK TO TOP ============================= -->
<button id="backToTop" class="back-to-top" aria-label="Back to top">
  <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round">
    <path d="M12 19V5"/>
    <path d="M6 11l6-6 6 6"/>
  </svg>
</button>
<nav class="section-dots" id="sectionDots" aria-label="Section navigation"></nav>
<!-- ============================= MOBILE STICKY CTA ============================= -->
<div class="mobile-cta-bar" id="mobileCtaBar">
  <a href="tel:+917002050242" class="btn btn-outline-dark" data-ripple>Call Now</a>
  <a href="#contact" class="btn btn-gold" data-ripple>Get Consultation</a>
</div>


<script>
/* ============================= DATA ============================= */
const registryData = [
  { title:"GST Services", items:["GST Registration","GST Amendment","GST Cancellation","GSTR-1 Filing","GSTR-3B Filing","GSTR-9 (Annual Return)","GSTR-9C","GST LUT Filing","GST Refund Processing","GST Notice Reply","GST Reconciliation","E-Way Bill Generation","GST Consultancy"] },
  { title:"EPFO & ESIC", items:["EPFO Registration","EPFO Monthly ECR Filing","UAN Activation","PF KYC Update","PF Transfer & Withdrawal Assistance","ESIC Registration","ESIC Monthly Return Filing","ESIC Employee Registration","ESIC Benefit Claims Assistance"] },
  { title:"Labour Law Compliance", items:["Professional Tax Registration","Professional Tax Return Filing","Labour License Registration","Shops & Establishment Registration","CLRA Registration","Contract Labour Compliance","Factory License Assistance","Labour Welfare Fund Compliance"] },
  { title:"Business Registrations", items:["Udyam (MSME) Registration","FSSAI Registration","FSSAI License Renewal","IEC (Import Export Code)","Trade License","Startup India Registration","GeM Registration","NSIC Registration","Digital Signature Certificate (DSC)","E-Sign Services"] },
  { title:"Company & LLP Services", items:["Private Limited Company Registration","LLP Registration","One Person Company (OPC) Registration","Partnership Firm Registration","Trust Registration","Society Registration","Section 8 Company Registration","Company Name Reservation (RUN)","DIN & DSC Application"] },
  { title:"ROC Compliance", items:["Annual ROC Filing","AOC-4 Filing","MGT-7 Filing","DIR-3 KYC","ADT-1 Filing","Board Resolution Drafting","Share Transfer Documentation","Company Strike Off","LLP Annual Filing"] },
  { title:"Income Tax Services", items:["ITR Filing (Individuals)","ITR Filing (Businesses)","Tax Planning","PAN Application","TAN Application","TDS Registration","TDS Return Filing","Form 16 Preparation","Income Tax Notice Reply"] },
  { title:"Accounting & Payroll", items:["Bookkeeping","Accounting Services","Payroll Processing","Salary Sheet Preparation","Bank Reconciliation","Financial Statements","Profit & Loss Statement","Balance Sheet Preparation","MIS Reporting"] },
  { title:"Licensing & Certifications", items:["ISO Certification","Trademark Registration","Trademark Renewal","Copyright Registration","Trademark Objection Reply","Barcode Registration","BIS Certification","AGMARK Registration"] },
  { title:"Digital Signature & Authentication", items:["Class 3 DSC","DGFT DSC","ICEGATE DSC","Document E-Signing","DSC Renewal"] },
  { title:"Other Professional Services", items:["Business Compliance Audit","Compliance Calendar Management","Virtual CFO Services","Company Compliance Management","Legal Documentation","Drafting Agreements","Vendor Registration","Tender Documentation","Project Report Preparation","Loan Documentation Assistance"] }
];

const faqData = [
  { q:"What are your service charges?", a:"Charges vary by service and business complexity — every quote is itemised upfront on the service page, with no hidden add-ons. Reach out for a tailored estimate." },
  { q:"How long does company registration take?", a:"Once documentation is complete, most private limited or LLP registrations are finalised within 10–15 working days, depending on MCA processing times." },
  { q:"What documents are needed for GST registration?", a:"PAN of the business, Aadhaar of directors/partners, address proof of the place of business, a bank statement, digital signature and a letter of authorisation." },
  { q:"Do you handle trademark objections?", a:"Yes — from search and filing through to responding to examination objections and securing the registration certificate." },
  { q:"Can you manage payroll for a distributed team?", a:"Yes, our payroll service covers salary processing, statutory deductions and compliance filings regardless of how many states your team is spread across." },
  { q:"Do you offer ongoing compliance monitoring, or only one-off filings?", a:"Both — most clients are on an ongoing retainer with a compliance calendar, renewal alerts, and a dedicated point of contact." }
];

const testimonials = [
  { text:"Samal Consultancy handles our GST and compliance without us ever having to chase them. Reliable, every cycle.", name:"Parbin Industries", role:"Manufacturing", initials:"PI" },
  { text:"From registration to ongoing filings, they've made compliance one less thing we worry about.", name:"Hill Queen", role:"Hospitality", initials:"HQ" },
  { text:"Straightforward, on time, and easy to reach whenever I have a question.", name:"Bimal Baruah", role:"Client", initials:"BB" },
  { text:"Their team understands agri-manufacturing compliance and keeps every filing on schedule.", name:"Satyam Vedany Agrotech", role:"Agro-Manufacturing", initials:"SV" },
  { text:"A dependable compliance partner for our contracting business — accurate and always ahead of deadlines.", name:"SBB Associates", role:"Contracting", initials:"SB" }
];

/* ============================= PAGE-LOAD PRELOADER ============================= */
const preloaderEl = document.getElementById('preloader');
function dismissPreloader(){
  if(!preloaderEl) return;
  preloaderEl.classList.add('done');
  setTimeout(()=> preloaderEl.remove(), 700);
}
if(document.readyState === 'complete'){ dismissPreloader(); }
else{
  window.addEventListener('load', dismissPreloader);
  // Safety net so a slow/blocked asset never traps the visitor behind the loader
  setTimeout(dismissPreloader, 2200);
}


/* ============================= NAVBAR SCROLL ============================= */
const navbar = document.getElementById('navbar');
const backToTop = document.getElementById('backToTop');
const mobileCtaBar = document.getElementById('mobileCtaBar');
const scrollProgress = document.getElementById('scrollProgress');
const prefersReduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
let lenis;
if(!prefersReduced && typeof Lenis !== 'undefined'){
  lenis = new Lenis({ duration:1.1, easing:t=>Math.min(1,1.001-Math.pow(2,-10*t)), smoothWheel:true });
  function raf(time){ lenis.raf(time); requestAnimationFrame(raf); }
  requestAnimationFrame(raf);
  lenis.on('scroll', ()=>{ lastKnownScrollY = window.scrollY; if(!ticking){ requestAnimationFrame(onScrollFrame); ticking = true; } });
}
const isCoarsePointer = window.matchMedia('(pointer: coarse)').matches;
const heroEl = document.querySelector('.hero');

/* Parallax targets, gathered once */
const parallaxEls = document.querySelectorAll('[data-parallax]');
const depthOrbs = document.querySelectorAll('[data-depth]');
const planeEls = document.querySelectorAll('[data-plane]');

let lastKnownScrollY = window.scrollY;
let ticking = false;
function onScrollFrame(){
  const y = lastKnownScrollY;
  navbar.classList.toggle('scrolled', y > 40);
  backToTop.classList.toggle('show', y > 600);
  mobileCtaBar.classList.toggle('show', y > 500);
  const max = document.documentElement.scrollHeight - window.innerHeight;
  scrollProgress.style.width = (max > 0 ? (y / max) * 100 : 0) + '%';
  const heroGridEl = document.querySelector('.hero-grid');
  if(heroGridEl && !prefersReduced){
    const heroH = heroEl ? heroEl.offsetHeight : window.innerHeight;
    const p = Math.min(y / heroH, 1);
    heroGridEl.style.transform = `translateY(${p*60}px) scale(${1 - p*0.06}) rotateX(${p*4}deg)`;
    heroGridEl.style.opacity = 1 - p*0.7;
  }


  if(!prefersReduced){
    parallaxEls.forEach(el=>{
      const rect = el.getBoundingClientRect();
      const speed = parseFloat(el.dataset.parallax) || 0.05;
      const offset = (rect.top - window.innerHeight/2) * speed;
      el.style.transform = (el.style.transform && el.style.transform.includes('rotate')) ? el.style.transform : `translateY(${offset}px)`;
      el.style.setProperty('--py-offset', offset + 'px');
    });
    depthOrbs.forEach(orb=>{
      const rect = orb.getBoundingClientRect();
      const speed = parseFloat(orb.dataset.depth) || 0.08;
      const offset = (rect.top) * speed;
      orb.style.transform = `translate3d(0, ${offset}px, 0)`;
    });

    /* Multi-plane depth: near moves fastest+largest, far moves slowest+subtlest */
    const planeSpeeds = { near:0.14, mid:0.08, far:0.03 };
    const planeScale  = { near:1.03, mid:1.015, far:1.0 };
    planeEls.forEach(el=>{
      const plane = el.dataset.plane;
      const speed = planeSpeeds[plane] ?? 0.06;
      const scale = planeScale[plane] ?? 1;
      const rect = el.getBoundingClientRect();
      const centerOffset = (rect.top + rect.height/2) - window.innerHeight/2;
      const offset = -centerOffset * speed;
      const rotX = Math.max(-4, Math.min(4, centerOffset * 0.006));
      el.style.transform = `translateY(${offset}px) scale(${scale}) rotateX(${rotX}deg)`;
    });
  }
  ticking = false;
}
let lastVeloY = window.scrollY, lastVeloTime = performance.now();
window.addEventListener('scroll', ()=>{
  lastKnownScrollY = window.scrollY;
  if(!ticking){ requestAnimationFrame(onScrollFrame); ticking = true; }

  if(!prefersReduced && !isCoarsePointer){
    const now = performance.now();
    const dt = now - lastVeloTime || 16;
    const velocity = Math.min(Math.abs(window.scrollY - lastVeloY) / dt, 3);
    document.querySelectorAll('.stats-band .stat-block').forEach((el,i)=>{
      el.style.transform = `translateY(${velocity * (i%2?3:-3)}px) rotateX(${velocity*2}deg)`;
    });
    lastVeloY = window.scrollY; lastVeloTime = now;
  }
}, { passive:true });
onScrollFrame();

/* ============================= VIEW-TRANSITION-STYLE ANCHOR NAV ============================= */
const routeBar = document.getElementById('routeBar');
function fauxRouteTransition(targetEl){
  if(prefersReduced){ return; }
  routeBar.style.width = '0%';
  routeBar.classList.add('active');
  requestAnimationFrame(()=> routeBar.style.width = '70%');

  document.body.classList.add('section-fade-target','fading');

  setTimeout(()=>{
    routeBar.style.width = '100%';
    document.body.classList.remove('fading');
    setTimeout(()=>{
      routeBar.classList.remove('active');
      routeBar.style.width = '0%';
    }, 300);
  }, 260);
}

document.querySelectorAll('a[href^="#"]').forEach(link=>{
  link.addEventListener('click', (e)=>{
    const id = link.getAttribute('href').slice(1);
    const target = document.getElementById(id);
    if(!target) return;
    fauxRouteTransition(target);
  });
});

const menuToggle = document.getElementById('menuToggle');
const navLinks = document.getElementById('navLinks');
menuToggle.addEventListener('click', () => {
  navLinks.classList.toggle('open');
  menuToggle.classList.toggle('open');
});
navLinks.querySelectorAll('a').forEach(a => a.addEventListener('click', () => {
  navLinks.classList.remove('open');
  menuToggle.classList.remove('open');
}));

backToTop.addEventListener('click', () => window.scrollTo({ top:0, behavior:'smooth' }));

/* ============================= RIPPLE EFFECT (touch + click) ============================= */
document.querySelectorAll('[data-ripple]').forEach(btn=>{
  btn.addEventListener('click', function(e){
    const rect = this.getBoundingClientRect();
    const span = document.createElement('span');
    const size = Math.max(rect.width, rect.height);
    span.className = 'ripple-span';
    span.style.width = span.style.height = size + 'px';
    span.style.left = (e.clientX - rect.left - size/2) + 'px';
    span.style.top = (e.clientY - rect.top - size/2) + 'px';
    this.appendChild(span);
    setTimeout(()=> span.remove(), 650);
  });
});

/* ============================= HERO INTERACTIVITY ============================= */
const cursorGlow = document.getElementById('cursorGlow');
const ledgerCard = document.querySelector('.ledger-card');

if(heroEl && !prefersReduced && !isCoarsePointer){
  const heroContentEl = document.getElementById('heroContent');
  const orbEls = document.querySelectorAll('.hero-orbs .orb');

  heroEl.addEventListener('mousemove', (e)=>{
    const rect = heroEl.getBoundingClientRect();
    const relX = e.clientX - rect.left;
    const relY = e.clientY - rect.top;
    const px = relX / rect.width;
    const py = relY / rect.height;

    if(cursorGlow){
      cursorGlow.style.left = relX + 'px';
      cursorGlow.style.top = relY + 'px';
    }

    if(ledgerCard){
      const cardRect = ledgerCard.getBoundingClientRect();
      const cx = cardRect.left + cardRect.width/2;
      const cy = cardRect.top + cardRect.height/2;
      const dx = (e.clientX - cx) / (cardRect.width/2);
      const dy = (e.clientY - cy) / (cardRect.height/2);
      const clampedX = Math.max(-1, Math.min(1, dx));
      const clampedY = Math.max(-1, Math.min(1, dy));
      ledgerCard.style.transform = `rotateY(${clampedX*8}deg) rotateX(${-clampedY*8}deg) translateZ(10px)`;
      ledgerCard.style.setProperty('--gx', ((e.clientX - cardRect.left) / cardRect.width * 100) + '%');
      ledgerCard.style.setProperty('--gy', ((e.clientY - cardRect.top) / cardRect.height * 100) + '%');
    }

    if(heroContentEl){
      const rx = (py - 0.5) * -6;
      const ry = (px - 0.5) * 6;
      heroContentEl.style.transform = `rotateX(${rx}deg) rotateY(${ry}deg)`;
    }

    orbEls.forEach((orb, i)=>{
      const depth = (i + 1) * 14;
      const ox = (px - 0.5) * depth;
      const oy = (py - 0.5) * depth;
      const scrollOffset = orb.style.getPropertyValue('--py-offset') || '0px';
      orb.style.transform = `translate3d(${ox}px, calc(${oy}px + ${scrollOffset}), 0)`;
    });
  });

  heroEl.addEventListener('mouseleave', ()=>{
    if(ledgerCard) ledgerCard.style.transform = 'rotateY(0deg) rotateX(0deg) translateZ(0)';
    if(heroContentEl) heroContentEl.style.transform = 'rotateX(0deg) rotateY(0deg)';
    orbEls.forEach(orb=> orb.style.transform = '');
  });

  /* Magnetic hero buttons */
  document.querySelectorAll('.hero-actions .btn, .footer-cta .btn, .btn-gold').forEach(btn=>{
    btn.addEventListener('mousemove', (e)=>{
      const r = btn.getBoundingClientRect();
      const mx = e.clientX - r.left - r.width/2;
      const my = e.clientY - r.top - r.height/2;
      btn.style.transform = `translate(${mx*0.18}px, ${my*0.35}px)`;
    });
    btn.addEventListener('mouseleave', ()=>{ btn.style.transform=''; });
  });
}

/* Floating ambient particles in hero */
const heroParticlesEl = document.getElementById('heroParticles');
if(heroParticlesEl && !prefersReduced){
  const colors = ['rgba(232,199,102,0.9)','rgba(143,160,232,0.85)','rgba(255,255,255,0.7)'];
  let particleHtml = '';
  for(let i=0;i<18;i++){
    const size = Math.random()*4+2;
    const left = Math.random()*100;
    const top = Math.random()*100;
    const dur = Math.random()*6+6;
    const delay = Math.random()*6;
    const color = colors[i % colors.length];
    particleHtml += `<span class="hero-particle" style="width:${size}px;height:${size}px;left:${left}%;top:${top}%;background:radial-gradient(circle, ${color}, transparent);animation-duration:${dur}s;animation-delay:${delay}s;"></span>`;
  }
  heroParticlesEl.innerHTML = particleHtml;
}

/* ============================= PRODUCT CARDS: 3D TILT + GLARE ============================= */
if(!prefersReduced && !isCoarsePointer){
  document.querySelectorAll('.prod-card[data-tilt]').forEach(card=>{
    card.addEventListener('mousemove', (e)=>{
      const r = card.getBoundingClientRect();
      const px = (e.clientX - r.left) / r.width;
      const py = (e.clientY - r.top) / r.height;
      card.style.transform = `rotateY(${(px - 0.5) * 9}deg) rotateX(${(0.5 - py) * 9}deg) translateY(-4px)`;
      card.style.setProperty('--mx', (px * 100) + '%');
      card.style.setProperty('--my', (py * 100) + '%');
    });
    card.addEventListener('mouseleave', ()=>{ card.style.transform = ''; });
  });

  /* Magnetic product action buttons */
  document.querySelectorAll('.prod-actions .btn').forEach(btn=>{
    btn.addEventListener('mousemove', (e)=>{
      const r = btn.getBoundingClientRect();
      const mx = e.clientX - r.left - r.width/2;
      const my = e.clientY - r.top - r.height/2;
      btn.style.transform = `translate(${mx*0.15}px, ${my*0.3}px)`;
    });
    btn.addEventListener('mouseleave', ()=>{ btn.style.transform=''; });
  });
}

/* ============================= PRODUCTS SECTION — EXTRA 3D FX ============================= */
const productsBandEl = document.getElementById('products');
const productsSpotlightEl = document.getElementById('productsSpotlight');
if(productsBandEl && productsSpotlightEl && !prefersReduced && !isCoarsePointer){
  productsSpotlightEl.classList.add('on');
  productsBandEl.addEventListener('mousemove', (e)=>{
    const r = productsBandEl.getBoundingClientRect();
    const px = ((e.clientX - r.left) / r.width) * 100;
    const py = ((e.clientY - r.top) / r.height) * 100;
    productsSpotlightEl.style.setProperty('--psx', px + '%');
    productsSpotlightEl.style.setProperty('--psy', py + '%');
  });
}

/* Ambient floating particles inside the products band */
const prodParticlesEl = document.getElementById('prodParticles');
if(prodParticlesEl && !prefersReduced){
  const pColors = ['rgba(232,199,102,0.9)','rgba(143,160,232,0.85)','rgba(255,255,255,0.6)'];
  let html = '';
  for(let i=0;i<22;i++){
    const size = Math.random()*3.5+2;
    const left = Math.random()*100;
    const top = Math.random()*100;
    const dur = Math.random()*7+7;
    const delay = Math.random()*7;
    const color = pColors[i % pColors.length];
    html += `<span class="prod-particle" style="width:${size}px;height:${size}px;left:${left}%;top:${top}%;background:radial-gradient(circle, ${color}, transparent);animation-duration:${dur}s;animation-delay:${delay}s;"></span>`;
  }
  prodParticlesEl.innerHTML = html;
}

/* Extra icon parallax based on card tilt (adds to existing tilt effect) */
if(!prefersReduced && !isCoarsePointer){
  document.querySelectorAll('.prod-card[data-tilt]').forEach(card=>{
    const icon = card.querySelector('.prod-icon');
    card.addEventListener('mousemove', (e)=>{
      if(!icon) return;
      const r = card.getBoundingClientRect();
      const px = (e.clientX - r.left) / r.width - 0.5;
      const py = (e.clientY - r.top) / r.height - 0.5;
      icon.style.transform = `translateZ(46px) translate(${px*10}px, ${py*10}px) rotate(${px*10}deg)`;
    });
    card.addEventListener('mouseleave', ()=>{ if(icon) icon.style.transform = ''; });
  });
}

/* ============================= LIVE LEDGER ============================= */
const ledgerItems = [
  { name:"GSTR-3B", sub:"Monthly Return · June", status:"done" },
  { name:"EPFO ECR", sub:"Provident Fund · June", status:"done" },
  { name:"ESIC Return", sub:"Employee Insurance · June", status:"done" },
  { name:"TDS Return", sub:"Q1 FY26", status:"pending" },
  { name:"DIR-3 KYC", sub:"Director Filing · Annual", status:"done" }
];
const ledgerRows = document.getElementById('ledgerRows');
function renderLedger(){
  ledgerRows.innerHTML = ledgerItems.map((it,i)=>`
    <div class="ledger-row" style="animation-delay:${i*0.12}s">
      <div class="ledger-check ${it.status==='pending'?'pending':''}">${it.status==='pending'?'…':'✓'}</div>
      <div class="ledger-name">${it.name}<small>${it.sub}</small></div>
      <div class="ledger-status ${it.status==='pending'?'pending':''}">${it.status==='pending'?'In review':'Filed'}</div>
      <div class="ledger-time">${it.status==='pending'?'ETA 2d':'Just now'}</div>
    </div>`).join('');
}
/* Small delay lets the skeleton read as an intentional loading state rather than a flash */
setTimeout(renderLedger, 380);
function tickClock(){
  const el = document.getElementById('ledgerClock');
  el.textContent = new Date().toLocaleTimeString('en-IN', { hour12:false });
}
tickClock();
setInterval(tickClock, 1000);

/* ============================= MARQUEE ============================= */
const marqueeItems = ["GST Filing","EPFO & ESIC","Company Registration","Trademark","ROC Compliance","Payroll","Virtual CFO","Income Tax","FSSAI","Udyam Registration"];
const track = document.getElementById('marqueeTrack');
track.innerHTML = [...marqueeItems, ...marqueeItems].map(t=>`<span class="marquee-item">${t}</span>`).join('');
let lastScrollTime = performance.now();
let lastScrollY2 = window.scrollY;
window.addEventListener('scroll', ()=>{
  const now = performance.now();
  const dt = now - lastScrollTime || 16;
  const dy = Math.abs(window.scrollY - lastScrollY2);
  const velocity = dy/dt;
  const speedFactor = Math.max(0.4, 1 - Math.min(velocity*2, 0.6));
  track.style.animationDuration = (28*speedFactor)+'s';
  lastScrollTime = now; lastScrollY2 = window.scrollY;
}, {passive:true});

/* ============================= STATS COUNTER ============================= */
const statEls = document.querySelectorAll('.stat-num');
function animateCount(el){
  const target = +el.dataset.count;
  const suffix = el.dataset.suffix || '';
  const dur = 1600, start = performance.now();
  const blockEl = el.closest('.stat-block');
  function step(now){
    const p = Math.min((now-start)/dur, 1);
    const eased = 1 - Math.pow(1-p, 3);
    const val = Math.floor(eased*target);
    el.innerHTML = val.toLocaleString('en-IN').split('').map((ch,i)=>
      `<span class="digit" style="transform:perspective(300px) rotateX(${(1-p)*160*(i%2?1:-1)}deg) translateZ(${(1-p)*20}px); opacity:${0.3+eased*0.7}">${ch}</span>`
    ).join('') + suffix;
    if(p<1) requestAnimationFrame(step);
    else{
      el.textContent = target.toLocaleString('en-IN') + suffix;
      if(blockEl) blockEl.classList.add('settle');
    }
  }
  requestAnimationFrame(step);
}
const statObserver = new IntersectionObserver((entries)=>{
  entries.forEach(e=>{
    if(e.isIntersecting){ animateCount(e.target); statObserver.unobserve(e.target); }
  });
},{threshold:0.5});
statEls.forEach(el=>statObserver.observe(el));

/* ============================= REGISTRY ACCORDION ============================= */
const accList = document.getElementById('accList');
accList.innerHTML = registryData.map((cat,i)=>`
  <div class="acc-item" data-idx="${i}">
    <button class="acc-head">
      <span class="acc-index">${String(i+1).padStart(2,'0')}</span>
      <h4>${cat.title}</h4>
      <span class="acc-count">${cat.items.length} services</span>
      <span class="acc-plus"></span>
    </button>
    <div class="acc-panel">
      <div class="acc-panel-inner">
        ${cat.items.map(it=>`<span class="chip">${it}</span>`).join('')}
      </div>
    </div>
  </div>
`).join('');

accList.querySelectorAll('.acc-item').forEach(item=>{
  const head = item.querySelector('.acc-head');
  const panel = item.querySelector('.acc-panel');
  head.addEventListener('click', ()=>{
    const isOpen = item.classList.contains('open');
    accList.querySelectorAll('.acc-item.open').forEach(o=>{
      o.classList.remove('open');
      o.querySelector('.acc-panel').style.maxHeight = null;
    });
    if(!isOpen){
      item.classList.add('open');
      panel.style.maxHeight = panel.scrollHeight + 'px';
    }
  });
});

/* ============================= FAQ ============================= */
const faqList = document.getElementById('faqList');
function renderFaq(list){
  faqList.innerHTML = list.map((f,i)=>`
    <div class="faq-item" data-idx="${i}">
      <div class="faq-flip">
        <button class="faq-q"><span>${f.q}</span><span class="fplus">+</span></button>
      </div>
      <div class="faq-a-panel"><p>${f.a}</p></div>
    </div>`).join('');
  faqList.querySelectorAll('.faq-item').forEach(item=>{
    const q = item.querySelector('.faq-q');
    q.addEventListener('click', ()=>{
      const isOpen = item.classList.contains('open');
      faqList.querySelectorAll('.faq-item.open').forEach(o=> o.classList.remove('open'));
      if(!isOpen) item.classList.add('open');
    });
  });
}
renderFaq(faqData);
document.getElementById('faqSearch').addEventListener('input', (e)=>{
  const val = e.target.value.toLowerCase();
  const filtered = faqData.filter(f => f.q.toLowerCase().includes(val) || f.a.toLowerCase().includes(val));
  if(!filtered.length){ faqList.innerHTML = '<p style="padding:20px 4px;color:#8F8B80;">No matching questions — try a different term.</p>'; }
  else{ renderFaq(filtered); }
});

/* ============================= TESTIMONIALS ============================= */

const testDots = document.getElementById('testDots');
/* ============================= 3D COVERFLOW TESTIMONIALS ============================= */
const cfStage = document.getElementById('coverflowStage');
function renderCoverflow(){
  cfStage.innerHTML = testimonials.map((t,i)=>`
    <div class="cf-card" data-idx="${i}">
      <span class="test-quote-mark">"</span>
      <p class="test-text">${t.text}</p>
      <div class="test-author">
        <div class="avatar-fallback">${t.initials}</div>
        <div class="author-meta"><h5>${t.name}</h5><span>${t.role}</span></div>
      </div>
    </div>`).join('');
  testDots.innerHTML = testimonials.map((_,i)=>`<button class="test-dot ${i===0?'active':''}" data-idx="${i}"></button>`).join('');
  testDots.querySelectorAll('.test-dot').forEach(dot=>{
    dot.addEventListener('click', ()=> showTest(+dot.dataset.idx));
  });
  cfStage.querySelectorAll('.cf-card').forEach(card=>{
    card.addEventListener('click', ()=>{
      const idx = +card.dataset.idx;
      if(!card.classList.contains('center')) showTest(idx);
    });
  });
}
renderCoverflow();
if(!prefersReduced && !isCoarsePointer){
  cfStage.addEventListener('mousemove', (e)=>{
    const center = cfStage.querySelector('.cf-card.center');
    if(!center) return;
    const r = center.getBoundingClientRect();
    const px = (e.clientX - r.left)/r.width - 0.5;
    const py = (e.clientY - r.top)/r.height - 0.5;
    center.style.transform = `translateX(0) translateZ(0) rotateY(${px*10}deg) rotateX(${-py*10}deg)`;
  });
  cfStage.addEventListener('mouseleave', ()=>{
    const center = cfStage.querySelector('.cf-card.center');
    if(center) center.style.transform = '';
  });
}

let currentTest = 0;
function showTest(i){
  const n = testimonials.length;
  const cards = cfStage.querySelectorAll('.cf-card');
  cards.forEach((card,idx)=>{
    card.classList.remove('center','left','right','hidden-l','hidden-r');
    const diff = (idx - i + n) % n;
    if(diff===0) card.classList.add('center');
    else if(diff===1) card.classList.add('right');
    else if(diff===n-1) card.classList.add('left');
    else if(diff===2) card.classList.add('hidden-r');
    else card.classList.add('hidden-l');
  });
  document.querySelectorAll('.test-dot').forEach((d,idx)=> d.classList.toggle('active', idx===i));
  currentTest = i;
}
showTest(0);
setInterval(()=>{ showTest((currentTest+1) % testimonials.length); }, 5500);

let touchStartX = 0;
const coverflowWrapEl = document.querySelector('.coverflow-wrap');
coverflowWrapEl.addEventListener('touchstart', e=>{ touchStartX = e.changedTouches[0].screenX; }, {passive:true});
coverflowWrapEl.addEventListener('touchend', e=>{
  const dx = e.changedTouches[0].screenX - touchStartX;
  if(Math.abs(dx) > 40){
    if(dx < 0) showTest((currentTest+1) % testimonials.length);
    else showTest((currentTest-1+testimonials.length) % testimonials.length);
  }
}, {passive:true});
setInterval(()=>{ showTest((currentTest+1) % testimonials.length); }, 5500);

/* Testimonial card: cursor glare + gentle 3D tilt */
const testCardEl = document.querySelector('.test-card[data-glare-3d]');
if(testCardEl && !prefersReduced && !isCoarsePointer){
  testCardEl.addEventListener('mousemove', (e)=>{
    const r = testCardEl.getBoundingClientRect();
    const px = (e.clientX - r.left) / r.width;
    const py = (e.clientY - r.top) / r.height;
    testCardEl.style.setProperty('--tx', (px * 100) + '%');
    testCardEl.style.setProperty('--ty', (py * 100) + '%');
    testCardEl.style.transform = `rotateY(${(px - 0.5) * 4}deg) rotateX(${(0.5 - py) * 4}deg)`;
  });
  testCardEl.addEventListener('mouseleave', ()=>{ testCardEl.style.transform = ''; });
}

/* ============================= CORE SERVICES: TILT + GLARE ============================= */
if(!prefersReduced && !isCoarsePointer){
  document.querySelectorAll('.core-card[data-glare]').forEach(card=>{
    card.addEventListener('mousemove', (e)=>{
      const r = card.getBoundingClientRect();
      const px = (e.clientX - r.left) / r.width;
      const py = (e.clientY - r.top) / r.height;
      card.style.setProperty('--mx', (px * 100) + '%');
      card.style.setProperty('--my', (py * 100) + '%');
      card.style.transform = `translateY(-6px) rotateY(${(px - 0.5) * 6}deg) rotateX(${(0.5 - py) * 6}deg)`;
    });
    card.addEventListener('mouseleave', ()=>{ card.style.transform = ''; });
  });

  /* Why Us cells: subtle 3D tilt */
  document.querySelectorAll('.why-cell').forEach(cell=>{
    cell.addEventListener('mousemove', (e)=>{
      const r = cell.getBoundingClientRect();
      const px = (e.clientX - r.left) / r.width;
      const py = (e.clientY - r.top) / r.height;
      cell.style.transform = `rotateY(${(px - 0.5) * 5}deg) rotateX(${(0.5 - py) * 5}deg)`;
    });
    cell.addEventListener('mouseleave', ()=>{ cell.style.transform = ''; });
  });
}
/* ============================= HOW IT WORKS v2 ============================= */
const hiwData = [
  { icon:'✎', title:'Share Details', desc:'Tell us your business type and compliance needs.', back:'A 10-minute conversation gives us everything we need to map the right filings to your business — no guesswork, no generic checklists.' },
  { icon:'⇪', title:'Upload Documents', desc:'Securely share the required documents on our portal.', back:'Encrypted upload, checklist-guided so you never miss a document — nothing sits unprotected in an inbox.' },
  { icon:'⌕', title:'Compliance Check', desc:'Our experts prepare and cross-verify every filing.', back:'Every filing is reviewed by a second set of eyes before it goes out — catching errors before they become penalties.' },
  { icon:'⇧', title:'Filing & Reporting', desc:'We file digitally and send you the acknowledgement.', back:'You get the acknowledgement the same day we file — full visibility, zero chasing.' },
  { icon:'◷', title:'Ongoing Support', desc:'We track renewals and handle regulatory queries.', back:'Your compliance calendar runs quietly in the background — renewals, due dates and notices, handled before they turn urgent.' }
];
const hiwTrack = document.getElementById('hiwTrack');
hiwTrack.insertAdjacentHTML('beforeend', hiwData.map((s,i)=>`
  <div class="hiw-card" style="--i:${i}" data-idx="${i}">
    <div class="hiw-inner">
      <div class="hiw-face hiw-front">
        <span class="hiw-num">0${i+1}</span>
        <div class="hiw-icon">${s.icon}</div>
        <h4>${s.title}</h4><p>${s.desc}</p>
        <span class="hiw-flip-hint">Flip</span>
      </div>
      <div class="hiw-face hiw-back"><h5>${s.title}</h5><p>${s.back}</p></div>
    </div>
  </div>`).join(''));

hiwTrack.querySelectorAll('.hiw-card').forEach(card=>{
  card.addEventListener('click', ()=> card.classList.toggle('flipped'));
});
if(!prefersReduced && !isCoarsePointer){
  hiwTrack.querySelectorAll('.hiw-card').forEach(card=>{
    const inner = card.querySelector('.hiw-inner');
    card.addEventListener('mousemove', (e)=>{
      if(card.classList.contains('flipped')) return;
      const r = card.getBoundingClientRect();
      const px = (e.clientX-r.left)/r.width, py = (e.clientY-r.top)/r.height;
      inner.style.transform = `rotateY(${(px-0.5)*10}deg) rotateX(${(0.5-py)*10}deg)`;
    });
    card.addEventListener('mouseleave', ()=>{ if(!card.classList.contains('flipped')) inner.style.transform=''; });
  });
}
const hiwLineFill = document.getElementById('hiwLineFill');
if(hiwLineFill){
  new IntersectionObserver((entries)=>{
    entries.forEach(e=>{ if(e.isIntersecting){ hiwLineFill.style.width='100%'; } });
  },{threshold:0.3}).observe(hiwTrack);
}

/* ============================= INDUSTRIES v2 ============================= */
const industriesData = [
  { icon:'⚙', title:'Manufacturers', desc:'Factory registrations, labour compliance & export documentation.' },
  { icon:'✚', title:'Hospitals', desc:'Biomedical licensing, staff PF/ESIC & regulatory renewals.' },
  { icon:'◈', title:'Hotels', desc:'FSSAI, trade licenses & payroll for seasonal staff.' },
  { icon:'⚒', title:'Contractors', desc:'Labour licensing, CLRA compliance & tender documentation.' },
  { icon:'🛍', title:'Retail & Traders', desc:'GST, Udyam registration & day-to-day bookkeeping.' },
  { icon:'🚀', title:'Startups', desc:'Company incorporation, Startup India & early-stage compliance.' }
];
const indGrid = document.getElementById('indGrid');
indGrid.innerHTML = industriesData.map((it,i)=>`
  <div class="ind-card" style="--i:${i}">
    <div class="ind-icon">${it.icon}</div>
    <h4>${it.title}</h4>
    <p class="ind-desc">${it.desc}</p>
  </div>`).join('');
if(!prefersReduced && !isCoarsePointer){
  indGrid.querySelectorAll('.ind-card').forEach(card=>{
    card.addEventListener('mousemove', (e)=>{
      const r = card.getBoundingClientRect();
      const px = (e.clientX-r.left)/r.width, py=(e.clientY-r.top)/r.height;
      card.style.setProperty('--mx',(px*100)+'%'); card.style.setProperty('--my',(py*100)+'%');
      card.style.transform = `translateY(-6px) rotateY(${(px-0.5)*10}deg) rotateX(${(0.5-py)*10}deg)`;
    });
    card.addEventListener('mouseleave', ()=>{ card.style.transform=''; });
  });
}
/* ============================= INDUSTRY / AREA CARDS: 3D TILT ============================= */
if(!prefersReduced && !isCoarsePointer){
  document.querySelectorAll('.industry-card').forEach(card=>{
    card.addEventListener('mousemove', (e)=>{
      const r = card.getBoundingClientRect();
      const px = (e.clientX - r.left) / r.width;
      const py = (e.clientY - r.top) / r.height;
      card.style.transform = `translateY(-6px) rotateY(${(px - 0.5) * 10}deg) rotateX(${(0.5 - py) * 10}deg)`;
    });
    card.addEventListener('mouseleave', ()=>{ card.style.transform = ''; });
  });
}

/* ============================= SCROLL REVEAL ============================= */
const revealEls = document.querySelectorAll('.reveal');
const revealObserver = new IntersectionObserver((entries)=>{
  entries.forEach(e=>{
    if(e.isIntersecting){
      e.target.classList.add('in');
      if(e.target.classList.contains('prod-grid')) e.target.classList.add('deck-in');
      revealObserver.unobserve(e.target);
    }
  });
}, { threshold:0.12 });
revealEls.forEach(el=>revealObserver.observe(el));

/* ============================= CONTACT FORM VALIDATION + SUBMIT ============================= */
const validators = {
  fname: v => v.trim().length >= 2 || 'Please enter your full name.',
  fphone: v => /^[0-9+\-\s]{7,15}$/.test(v.trim()) || 'Enter a valid phone number.',
  femail: v => /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(v.trim()) || 'Enter a valid email address.',
  fmsg: v => v.trim().length >= 8 || 'Tell us a little more about what you need.'
};
function validateField(id){
  const input = document.getElementById(id);
  const field = input.closest('.field');
  const errEl = document.getElementById(id + '-err');
  const result = validators[id](input.value);
  field.classList.remove('invalid','valid','animation-none');
  if(result === true){
    field.classList.add('valid');
    errEl.textContent = '';
    return true;
  }
  // restart the shake animation even on repeated invalid submissions
  void field.offsetWidth;
  field.classList.add('invalid');
  errEl.textContent = result;
  return false;
}
['fname','fphone','femail','fmsg'].forEach(id=>{
  const input = document.getElementById(id);
  input.addEventListener('blur', ()=> validateField(id));
  input.addEventListener('input', ()=>{
    const field = input.closest('.field');
    if(field.classList.contains('invalid')) validateField(id);
  });
});

document.getElementById('contactForm').addEventListener('submit', async function(e){
  e.preventDefault();
  const form = this;
  const btn = document.getElementById('contactSubmitBtn');
  const statusEl = document.getElementById('formStatus');
  const original = btn.innerHTML;

  // Honeypot check — if a bot filled the hidden field, quietly stop.
  if (document.getElementById('fwebsite').value.trim() !== '') return;

  const allValid = ['fname','fphone','femail','fmsg'].map(validateField).every(Boolean);
  if(!allValid){
    statusEl.style.display = 'block';
    statusEl.className = 'form-status error';
    statusEl.textContent = 'Please fix the highlighted fields before sending.';
    return;
  }

  btn.disabled = true;
  btn.innerHTML = 'Sending…';
  statusEl.style.display = 'none';

  try {
    const res = await fetch('api/contact-submit.php', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        name: document.getElementById('fname').value.trim(),
        phone: document.getElementById('fphone').value.trim(),
        email: document.getElementById('femail').value.trim(),
        message: document.getElementById('fmsg').value.trim(),
        website: document.getElementById('fwebsite').value
      })
    });
    const data = await res.json();

    statusEl.style.display = 'block';
    if (data.success) {
      statusEl.className = 'form-status success';
      statusEl.textContent = data.message;
      form.reset();
      ['fname','fphone','femail','fmsg'].forEach(id=> document.getElementById(id).closest('.field').classList.remove('valid','invalid'));
    } else {
      statusEl.className = 'form-status error';
      statusEl.textContent = data.message || 'Something went wrong. Please try again or call us directly.';
    }
  } catch (err) {
    statusEl.style.display = 'block';
    statusEl.className = 'form-status error';
    statusEl.textContent = 'Network error — please try again or call us directly.';
  } finally {
    btn.disabled = false;
    btn.innerHTML = original;
  }
});

/* Contact panel: cursor-tracked glare */
const contactPanelEl = document.querySelector('.contact-panel[data-glare-panel]');
if(contactPanelEl && !prefersReduced && !isCoarsePointer){
  contactPanelEl.addEventListener('mousemove', (e)=>{
    const r = contactPanelEl.getBoundingClientRect();
    const px = ((e.clientX - r.left) / r.width) * 100;
    const py = ((e.clientY - r.top) / r.height) * 100;
    contactPanelEl.style.setProperty('--px', px + '%');
    contactPanelEl.style.setProperty('--py', py + '%');
  });
}

/* Magnetic submit button */
const submitBtnEl = document.querySelector('#contactForm button[type="submit"]');
if(submitBtnEl && !prefersReduced && !isCoarsePointer){
  submitBtnEl.addEventListener('mousemove', (e)=>{
    const r = submitBtnEl.getBoundingClientRect();
    const mx = e.clientX - r.left - r.width/2;
    const my = e.clientY - r.top - r.height/2;
    submitBtnEl.style.transform = `translate(${mx*0.15}px, ${my*0.3}px)`;
  });
  submitBtnEl.addEventListener('mouseleave', ()=>{ submitBtnEl.style.transform = ''; });
}

/* ============================= NEWSLETTER SUBSCRIBE ============================= */
document.getElementById('newsletterForm').addEventListener('submit', async function(e){
  e.preventDefault();
  const form = this;
  const btn = document.getElementById('newsletterBtn');
  const statusEl = document.getElementById('newsletterStatus');
  const emailInput = document.getElementById('newsletterEmail');
  const original = btn.textContent;

  btn.disabled = true;
  btn.textContent = '...';
  statusEl.style.display = 'none';

  try {
    const res = await fetch('api/newsletter-subscribe.php', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ email: emailInput.value.trim() })
    });
    const data = await res.json();

    statusEl.style.display = 'block';
    statusEl.style.color = data.success ? '#4ADE80' : '#FCA5A5';
    statusEl.textContent = data.message;
    if (data.success) form.reset();
  } catch (err) {
    statusEl.style.display = 'block';
    statusEl.style.color = '#FCA5A5';
    statusEl.textContent = 'Network error — please try again.';
  } finally {
    btn.disabled = false;
    btn.textContent = original;
  }
});

/* ============================= FOOTER: CURSOR-TRACKED AMBIENT GLOW ============================= */
const footerEl = document.querySelector('footer');
if(footerEl && !prefersReduced && !isCoarsePointer){
  footerEl.addEventListener('mousemove', (e)=>{
    const r = footerEl.getBoundingClientRect();
    const fx = ((e.clientX - r.left) / r.width) * 100;
    const fy = ((e.clientY - r.top) / r.height) * 100;
    footerEl.style.setProperty('--fx', fx + '%');
    footerEl.style.setProperty('--fy', fy + '%');
  });
}

/* ============================= FOOTER YEAR ============================= */
document.getElementById('year').textContent = new Date().getFullYear();

const spotlightEl = document.getElementById('globalSpotlight');
const lensGridEl = document.getElementById('lensGrid');
const lensRingEl = document.getElementById('lensRing');
if(spotlightEl && !prefersReduced && !isCoarsePointer){
  spotlightEl.classList.add('on');
  lensGridEl.classList.add('on');
  lensRingEl.classList.add('on');

  window.addEventListener('mousemove', (e)=>{
    spotlightEl.style.setProperty('--sx', e.clientX + 'px');
    spotlightEl.style.setProperty('--sy', e.clientY + 'px');
    lensGridEl.style.setProperty('--sx', e.clientX + 'px');
    lensGridEl.style.setProperty('--sy', e.clientY + 'px');
  }, { passive:true });

  /* Lens expands over cards/buttons, shrinks over plain text */
  document.querySelectorAll('.core-card, .prod-card, .why-cell, .industry-card, .cf-card').forEach(el=>{
    el.addEventListener('mouseenter', ()=>{
      lensRingEl.style.width = '340px'; lensRingEl.style.height = '340px';
    });
    el.addEventListener('mouseleave', ()=>{
      lensRingEl.style.width = '220px'; lensRingEl.style.height = '220px';
    });
  });

  /* Magnetic nudge: nearby chips/badges lean toward the cursor */
  let magTicking = false;
  window.addEventListener('mousemove', (e)=>{
    if(magTicking) return;
    magTicking = true;
    requestAnimationFrame(()=>{
      document.querySelectorAll('.chip, .prod-chip').forEach(el=>{
        const r = el.getBoundingClientRect();
        const cx = r.left + r.width/2, cy = r.top + r.height/2;
        const dx = e.clientX - cx, dy = e.clientY - cy;
        const dist = Math.sqrt(dx*dx + dy*dy);
        if(dist < 140){
          const pull = (1 - dist/140) * 6;
          el.style.transform = `translate(${(dx/dist)*pull}px, ${(dy/dist)*pull}px)`;
        } else {
          el.style.transform = '';
        }
      });
      magTicking = false;
    });
  }, { passive:true });
}

const cursorDot = document.getElementById('cursorDot');
const cursorRing = document.getElementById('cursorRing');
const cursorRingLabel = document.getElementById('cursorRingLabel');
if(cursorDot && cursorRing && !prefersReduced && window.matchMedia('(pointer: fine)').matches){
  document.documentElement.classList.add('has-custom-cursor');
  let mx=0,my=0, rx=0, ry=0;
  window.addEventListener('mousemove', (e)=>{
    mx = e.clientX; my = e.clientY;
    cursorDot.style.left = mx+'px'; cursorDot.style.top = my+'px';
  }, { passive:true });
  function ringLoop(){
    rx += (mx-rx)*0.18; ry += (my-ry)*0.18;
    cursorRing.style.left = rx+'px'; cursorRing.style.top = ry+'px';
    requestAnimationFrame(ringLoop);
  }
  ringLoop();

  const modeClasses = ['mode-view','mode-card','mode-text','mode-drag'];
  function setCursorMode(mode, label){
    cursorRing.classList.remove(...modeClasses);
    cursorRing.classList.add('hover');
    if(mode) cursorRing.classList.add(mode);
    cursorRingLabel.textContent = label || '';
  }
  function resetCursor(){
    cursorRing.classList.remove('hover', ...modeClasses);
    cursorRingLabel.textContent = '';
  }

  document.querySelectorAll('a.btn, button.btn, .btn').forEach(el=>{
    el.addEventListener('mouseenter', ()=> setCursorMode('mode-view', 'View'));
    el.addEventListener('mouseleave', resetCursor);
  });
  document.querySelectorAll('.core-card, .prod-card, .why-cell, .industry-card, .cf-card').forEach(el=>{
    el.addEventListener('mouseenter', ()=> setCursorMode('mode-card', 'Explore'));
    el.addEventListener('mouseleave', resetCursor);
  });
  document.querySelectorAll('input, textarea').forEach(el=>{
    el.addEventListener('mouseenter', ()=> setCursorMode('mode-text', ''));
    el.addEventListener('mouseleave', resetCursor);
  });
  document.querySelectorAll('.chip, .prod-chip, .test-dot').forEach(el=>{
    el.addEventListener('mouseenter', ()=> setCursorMode('mode-drag', ''));
    el.addEventListener('mouseleave', resetCursor);
  });
  document.querySelectorAll('a:not(.btn)').forEach(el=>{
    el.addEventListener('mouseenter', ()=> cursorRing.classList.add('hover'));
    el.addEventListener('mouseleave', ()=> cursorRing.classList.remove('hover'));
  });
}

if(!prefersReduced && !isCoarsePointer){
  document.querySelectorAll('.value-item, .stat-block, .c-row').forEach(el=>{
    el.addEventListener('mousemove', (e)=>{
      const r = el.getBoundingClientRect();
      const px = (e.clientX - r.left) / r.width;
      const py = (e.clientY - r.top) / r.height;
      el.style.transform = `rotateY(${(px-0.5)*5}deg) rotateX(${(0.5-py)*5}deg) translateZ(4px)`;
    });
    el.addEventListener('mouseleave', ()=>{ el.style.transform = ''; });
  });
}

document.querySelectorAll('.core-card').forEach(card=>{
  card.addEventListener('click', (e)=>{
    if(e.target.closest('.core-link')) return;
    card.classList.toggle('flipped');
  });
});

if(!prefersReduced && !isCoarsePointer){
  document.querySelectorAll('.nav-links a').forEach(link=>{
    link.addEventListener('mousemove', (e)=>{
      const r = link.getBoundingClientRect();
      const mx = e.clientX - r.left - r.width/2;
      link.style.transform = `translateX(${mx*0.25}px)`;
    });
    link.addEventListener('mouseleave', ()=>{ link.style.transform=''; });
  });
  const brandEl = document.querySelector('.navbar .brand');
  if(brandEl){
    brandEl.addEventListener('mousemove', (e)=>{
      const r = brandEl.getBoundingClientRect();
      const px = (e.clientX - r.left) / r.width;
      const py = (e.clientY - r.top) / r.height;
      const logo = brandEl.querySelector('.brand-logo, .brand-mark');
      if(logo) logo.style.transform = `rotateY(${(px-0.5)*24}deg) rotateX(${(0.5-py)*24}deg)`;
    });
    brandEl.addEventListener('mouseleave', ()=>{
      const logo = brandEl.querySelector('.brand-logo, .brand-mark');
      if(logo) logo.style.transform = '';
    });
  }
}

if(!prefersReduced && !isCoarsePointer){
  faqList.addEventListener('mousemove', (e)=>{
    const item = e.target.closest('.faq-item');
    if(!item) return;
    const r = item.getBoundingClientRect();
    item.style.setProperty('--qx', ((e.clientX - r.left)/r.width*100) + '%');
    item.style.setProperty('--qy', ((e.clientY - r.top)/r.height*100) + '%');
  });
}

const dotSections = [
  {id:'top', label:'Home'}, {id:'about', label:'About'}, {id:'services', label:'Services'},
  {id:'registry', label:'Registry'}, {id:'process', label:'Process'}, {id:'clients', label:'Clients'},
  {id:'contact', label:'Contact'}, {id:'products', label:'Products'}
];
const sectionDotsEl = document.getElementById('sectionDots');
sectionDotsEl.innerHTML = dotSections.map(s=>`<a href="#${s.id}" data-label="${s.label}" data-target="${s.id}"></a>`).join('');
const dotEls = sectionDotsEl.querySelectorAll('a');
const dotObserver = new IntersectionObserver((entries)=>{
  entries.forEach(e=>{
    if(e.isIntersecting){
      dotEls.forEach(d=>d.classList.remove('active'));
      const match = sectionDotsEl.querySelector(`[data-target="${e.target.id}"]`);
      if(match) match.classList.add('active');
    }
  });
},{threshold:0.4});
dotSections.forEach(s=>{
  const el = document.getElementById(s.id);
  if(el) dotObserver.observe(el);
});

function spawnDust(x,y){
  for(let i=0;i<10;i++){
    const p = document.createElement('div');
    p.className='dust-particle';
    const size = Math.random()*6+3;
    const angle = Math.random()*Math.PI*2;
    const dist = Math.random()*60+30;
    p.style.width=p.style.height=size+'px';
    p.style.left=x+'px'; p.style.top=y+'px';
    p.style.setProperty('--dx', Math.cos(angle)*dist+'px');
    p.style.setProperty('--dy', Math.sin(angle)*dist+'px');
    document.body.appendChild(p);
    setTimeout(()=>p.remove(), 750);
  }
}
document.querySelectorAll('.btn-gold[data-ripple]').forEach(btn=>{
  btn.addEventListener('click', (e)=>{ if(!prefersReduced) spawnDust(e.clientX, e.clientY); });
});

/* ============================= 3D PARTICLE NETWORK (Three.js) ============================= */
(function initHero3D(){
  const canvas = document.getElementById('hero3d');
  if(!canvas || typeof THREE === 'undefined' || prefersReduced) return;

  const renderer = new THREE.WebGLRenderer({ canvas, alpha:true, antialias:true });
  const scene = new THREE.Scene();
  const camera = new THREE.PerspectiveCamera(60, heroEl.offsetWidth/heroEl.offsetHeight, 0.1, 1000);
  camera.position.z = 34;

  function resize3D(){
    const w = heroEl.offsetWidth, h = heroEl.offsetHeight;
    renderer.setSize(w, h);
    renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));
    camera.aspect = w/h;
    camera.updateProjectionMatrix();
  }
  resize3D();
  window.addEventListener('resize', resize3D);

  // Point cloud
  const COUNT = window.innerWidth < 768 ? 90 : 200;
  const positions = new Float32Array(COUNT*3);
  const speeds = [];
  for(let i=0;i<COUNT;i++){
    positions[i*3]   = (Math.random()-0.5)*70;
    positions[i*3+1] = (Math.random()-0.5)*40;
    positions[i*3+2] = (Math.random()-0.5)*40;
    speeds.push((Math.random()*0.15)+0.03);
  }
  const geo = new THREE.BufferGeometry();
  geo.setAttribute('position', new THREE.BufferAttribute(positions,3));
  const mat = new THREE.PointsMaterial({ color:0xE8C766, size:0.55, transparent:true, opacity:0.85 });
  const points = new THREE.Points(geo, mat);
  scene.add(points);

  // Connecting lines between nearby points
  const lineGeo = new THREE.BufferGeometry();
  const lineMat = new THREE.LineBasicMaterial({ color:0x8FA0E8, transparent:true, opacity:0.18 });
  const lineSegs = new THREE.LineSegments(lineGeo, lineMat);
  scene.add(lineSegs);

  function updateLines(){
    const verts = [];
    const maxDist = 9;
    for(let i=0;i<COUNT;i++){
      for(let j=i+1;j<COUNT;j++){
        const dx = positions[i*3]-positions[j*3];
        const dy = positions[i*3+1]-positions[j*3+1];
        const dz = positions[i*3+2]-positions[j*3+2];
        const d = Math.sqrt(dx*dx+dy*dy+dz*dz);
        if(d < maxDist){
          verts.push(positions[i*3],positions[i*3+1],positions[i*3+2]);
          verts.push(positions[j*3],positions[j*3+1],positions[j*3+2]);
        }
      }
    }
    lineGeo.setAttribute('position', new THREE.Float32BufferAttribute(verts,3));
  }
  updateLines();

  let mouseX = 0, mouseY = 0, frame = 0;
  let scrollRipple = 0, targetRipple = 0;
  heroEl.addEventListener('mousemove', (e)=>{
    const r = heroEl.getBoundingClientRect();
    mouseX = ((e.clientX - r.left)/r.width - 0.5);
    mouseY = ((e.clientY - r.top)/r.height - 0.5);
  }, { passive:true });

  window.addEventListener('scroll', ()=>{
    const heroH = heroEl.offsetHeight;
    const p = Math.min(Math.max(window.scrollY / heroH, 0), 1);
    targetRipple = p;
  }, { passive:true });

  const basePositions = positions.slice();

  function animate(){
    requestAnimationFrame(animate);
    frame++;

    scrollRipple += (targetRipple - scrollRipple) * 0.06;

    for(let i=0;i<COUNT;i++){
      const bx = basePositions[i*3], by = basePositions[i*3+1], bz = basePositions[i*3+2];
      const dist = Math.sqrt(bx*bx + by*by);
      // liquid ripple: radial wave that grows with scroll depth, plus gentle idle bob
      const rippleWave = Math.sin(dist*0.35 - frame*0.045 - scrollRipple*6) * scrollRipple * 5;
      const idleBob = Math.sin(frame*0.01 + i)*0.004;
      positions[i*3]   = bx + (bx/(dist||1)) * rippleWave * 0.6;
      positions[i*3+1] = by + idleBob + rippleWave;
      positions[i*3+2] = bz + Math.cos(dist*0.3 - frame*0.03) * scrollRipple * 6;
    }
    geo.attributes.position.needsUpdate = true;
    if(frame % 6 === 0) updateLines();

    // liquid-glass: points swell and fade as scroll ripple intensifies, lines dissolve
    mat.size = 0.55 + scrollRipple * 0.9;
    mat.opacity = Math.max(0.15, 0.85 - scrollRipple * 0.7);
    lineMat.opacity = Math.max(0, 0.18 - scrollRipple * 0.25);

    points.rotation.y += 0.0009 + scrollRipple*0.004;
    points.rotation.z = scrollRipple * 0.15;
    lineSegs.rotation.y = points.rotation.y;
    lineSegs.rotation.z = points.rotation.z;

    camera.position.x += (mouseX*6 - camera.position.x) * 0.02;
    camera.position.y += (-mouseY*4 - camera.position.y) * 0.02;
    camera.position.z = 34 + scrollRipple * 14;
    camera.lookAt(scene.position);

    renderer.render(scene, camera);
  }
  animate();
  requestAnimationFrame(()=> canvas.classList.add('ready'));

  window.addEventListener('scroll', ()=>{
    const heroH = heroEl.offsetHeight;
    const p = Math.min(Math.max(window.scrollY / heroH, 0), 1);
    canvas.style.filter = `blur(${p*3}px) saturate(${1 + p*0.6})`;
  }, { passive:true });
})();

(function initAreaGlobe(){
  const canvas = document.getElementById('areaGlobe');
  if(!canvas || typeof THREE === 'undefined' || prefersReduced) return;

  const size = canvas.parentElement.offsetWidth < 640 ? 170 : 220;
  const renderer = new THREE.WebGLRenderer({ canvas, alpha:true, antialias:true });
  renderer.setSize(size, size);
  renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));

  const scene = new THREE.Scene();
  const camera = new THREE.PerspectiveCamera(45, 1, 0.1, 100);
  camera.position.z = 6;

  const globeGeo = new THREE.SphereGeometry(2, 24, 24);
  const wireMat = new THREE.MeshBasicMaterial({ color:0x8FA0E8, wireframe:true, transparent:true, opacity:0.35 });
  const globe = new THREE.Mesh(globeGeo, wireMat);
  scene.add(globe);

  const dotMat = new THREE.MeshBasicMaterial({ color:0xE8C766 });
  const cities = 6;
  for(let i=0;i<cities;i++){
    const phi = Math.acos(-1 + (2*i)/cities);
    const theta = Math.sqrt(cities*Math.PI)*phi;
    const dot = new THREE.Mesh(new THREE.SphereGeometry(0.07,8,8), dotMat);
    dot.position.setFromSphericalCoords(2, phi, theta);
    globe.add(dot);
  }

  let obsGlobe = new IntersectionObserver((entries)=>{
    entries.forEach(e=>{ if(e.isIntersecting) animateGlobe(); });
  },{threshold:0.2});
  obsGlobe.observe(canvas);

  let running = false;
  function animateGlobe(){
    if(running) return;
    running = true;
    function loop(){
      globe.rotation.y += 0.006;
      globe.rotation.x = 0.3;
      renderer.render(scene, camera);
      requestAnimationFrame(loop);
    }
    loop();
  }
})();

(function initOrbit(){
  const orbit = document.getElementById('trustOrbit');
  if(!orbit) return;
  const icons = ['◈','◆','◇','◉','✦'];
  const radius = 68;
  let html = '<div class="trust-orbit-hub">30+ yrs</div>';
  icons.forEach((icon,i)=>{
    const angle = (i / icons.length) * 360;
    html += `<div class="trust-orbit-node" style="transform:rotateY(${angle}deg) translateZ(${radius}px);">${icon}</div>`;
  });
  orbit.innerHTML = html;
})();

(function initStatDepth(){
  if(prefersReduced || isCoarsePointer) return;
  const statBlocks = document.querySelectorAll('.stat-block');
  const statsBandEl = document.querySelector('.stats-band');
  if(!statsBandEl || !statBlocks.length) return;
  statsBandEl.addEventListener('mousemove', (e)=>{
    const r = statsBandEl.getBoundingClientRect();
    const px = (e.clientX - r.left)/r.width - 0.5;
    const py = (e.clientY - r.top)/r.height - 0.5;
    statBlocks.forEach((el,i)=>{
      const depth = (i+1)*6;
      el.style.transform = `translate3d(${px*depth}px, ${py*depth}px, 0)`;
    });
  });
  statsBandEl.addEventListener('mouseleave', ()=>{
    statBlocks.forEach(el=> el.style.transform = '');
  });
})();



(function initGyroTilt(){
  if(!isCoarsePointer || prefersReduced || !ledgerCard) return;
  function handleOrientation(e){
    const gamma = e.gamma || 0; // left-right tilt
    const beta = e.beta || 0;   // front-back tilt
    const rx = Math.max(-10, Math.min(10, (beta-40)*0.3));
    const ry = Math.max(-10, Math.min(10, gamma*0.3));
    ledgerCard.style.transform = `rotateY(${ry}deg) rotateX(${-rx}deg)`;
  }
  if(typeof DeviceOrientationEvent !== 'undefined' && typeof DeviceOrientationEvent.requestPermission === 'function'){
    heroEl.addEventListener('touchstart', function reqPerm(){
      DeviceOrientationEvent.requestPermission().then(state=>{
        if(state === 'granted') window.addEventListener('deviceorientation', handleOrientation);
      }).catch(()=>{});
      heroEl.removeEventListener('touchstart', reqPerm);
    }, { once:true });
  } else if(typeof DeviceOrientationEvent !== 'undefined'){
    window.addEventListener('deviceorientation', handleOrientation);
  }
})();

function attachTilt3D(el, {max=8, glare=true} = {}){
  if(prefersReduced || isCoarsePointer) return;
  el.addEventListener('mousemove', e=>{
    const r = el.getBoundingClientRect();
    const px = (e.clientX-r.left)/r.width, py = (e.clientY-r.top)/r.height;
    el.style.transform = `perspective(900px) rotateY(${(px-0.5)*max}deg) rotateX(${(0.5-py)*max}deg) translateZ(6px)`;
    if(glare){ el.style.setProperty('--mx',(px*100)+'%'); el.style.setProperty('--my',(py*100)+'%'); }
  });
  el.addEventListener('mouseleave', ()=> el.style.transform='');
}
document.querySelectorAll('[data-tilt-3d]').forEach(el=> attachTilt3D(el));

</script>
</body>
</html>